node_modules/
.next/
# 本地生产构建验证和审查用的构建目录（NEXT_DIST_DIR），别提交
.next-verify*/
.next-review-*/
dist/
build/
coverage/
*.log
.DS_Store
.env
.env.local
/env.local
.env.*.local
apps/api/data/
.reasonix/
*.tsbuildinfo
tools/
/outputs/
# codex 的工作留档（改动前后快照、补丁、回滚脚本、截图）。
# 只对本机有用，505 个文件 18MB，提交上去会永久留在仓库历史里删不掉。
/.audit/
docs/交接文档*

# 运行时上传的图片：客户/员工传上来的货物照、签收凭证、入库单。
# ⚠️ 2026-08-29：我用 `git add -A` 把 4 张这种图误提交进了提交 985e73f
#（其中一张 420KB），而且还跟老板说「工作区干净」—— 第九轮复核抓出来的。
# 这些是**运行时数据**，不是代码，本来就该进持久化存储、由 git 忽略。
# ⚠️ 仓库里**已经跟踪着 42 张**同类图片（历史遗留），这条 ignore 管不到它们；
#   那 42 张要不要清、怎么清，等老板拍板，别自作主张删。
data/images/
