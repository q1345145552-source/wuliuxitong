import type { MinimalHttpApp } from "../../server";

/**
 * 代理品牌接口（2026-09-16 空壳，B4 填内容）：
 * GET /auth/brand?host=&slug=（公开，只回 name + logoUrl）、GET /client/brand。
 */
export function registerBrandingRoutes(_app: MinimalHttpApp): void {
  // B4
}
