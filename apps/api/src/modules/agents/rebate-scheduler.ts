/**
 * 每月返现单生成（2026-09-16 空壳，B2 填内容）。
 * 设计：启动跑一次、之后每小时；照 exchange-rate/rate-sync.ts 的写法。
 * ⚠️ 空壳阶段什么都不做、不开定时器 —— main.ts 启动时已经在调它，B2 只需要填函数体。
 */
export function startAgentRebateScheduler(): void {
  // B2：生成已过完月份的返现单
}
