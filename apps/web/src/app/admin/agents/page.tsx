"use client";

// 代理管理占位页（B2 替换内容）。外壳（左边菜单、登录核验、角色闸）由根布局的 WorkbenchFrame 统一套上。
export default function AdminAgentsPage() {
  return (
    <section style={{ padding: 24 }}>
      <p style={{ color: "var(--t-muted)", fontSize: 14 }}>建设中</p>
    </section>
  );
}
