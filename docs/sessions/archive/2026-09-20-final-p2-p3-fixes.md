# 两个 P2 与三项 P3 修复

## 目标与边界
- 用户明确授权直接修复，修后交 DeepSeek 核查。只本地，不提交/推送/上线，不改建单输入规则、数据库、依赖或已执行发布包。
- 修正式标签连续空格显示、管理员总览写后刷新；清理无消费者时效聚合/响应类型、死 KPI 样式，补 NextLink 测试入口边界注释。
- 已读项目规范、现有记忆和 diagnose 技能。项目缺 INDEX/ADR/glossary/Agent skills 配置，本次依现有交接执行，不为局部修复修改协作规范或引入 issue-tracker 脚手架。

## 证据与诊断
- 基线 HEAD/所有文件哈希与8个目标原件保存在 `.audit/2026-09-20-final-p2-p3-fixes/`。
- 上一轮真实写回调已确定单飞提前返回会吞刷新；本轮将原复现转为回归先红再绿，不重复猜测已证实成因。普通轮询不排队；写后刷新合并排队并等待新结果，过期结果不覆盖，失败/超时也不能吞掉排队刷新。
- 连续空格：真实编码已保留原值；补真实 HTML 测试及浏览器文字空格宽度前后对照，只改运单号 span 的 white-space，不动其他字段。
- 时效查询是无当前界面消费者的冗余工作；不宣称未测得的生产耗时。现有旧包 probe 未调用 overview，不修改或放宽它；移除字段同步 API 类型与真实 handler 回归。

## 实施与验证
- 两个标签分支仅运单 span 增加 tracking-no + break-spaces。无编号规范化；条码 helper 与建单入口不变。
- loadOverview 用单飞共享完成 Promise + 写后 pending 位循环补发，三个写调用点显式 true。普通轮询不排队；跳过已失效旧数据/旧错误，最终失败才拒绝等待者。先注册 Promise 再调用服务，清理在同一微任务完成，防止同步异常/完成边界丢刷新。
- 删除趋势 SQL/映射/响应字段与前端类型，API 文档说明；保留全部计数和 stalledContainers SQL。删除 globals/ledger 中明确无引用的 KPI 规则，NextLink 注释明确 pages 入口与 App Router alias 边界。
- 首轮新测试旧源码：条码41/42；看板42/53（原42全通过，11个新增失败）。P2修后49/53；P3修后53/53；再补同步抛错/微任务边界，最终55/55。条码最终42/42。
- 固定旧源码隔离基线与修后全套：各46个 npm test命令（AI数字一项内部跑两个时区），scripts/API/Web三套tsc exit0；修后另跑默认Turbopack构建30/30。三项独立DB测试与agent-portal的真接口段明确跳过，不冒充真库验证。8个副本变异都被测试抓住，逐项恢复，恢复后看板/条码重跑全绿。
- IAB实际标签HTML前后：旧innerText为一个空格，第二空格宽0；新single与multi均保留两个空格，每个约3.90px。实际Next隔离夹具执行真loader/两种建单回调，验证旧成功/旧失败两种时序，页面最终3→4员工、111→112客户；控制台warning/error为0。

## 收尾
- 只用合成内存API/静态标签，未连业务/生产/数据库、未实物打印扫码。临时56495/3043已停、预览标签已关，原3000/3001 PID73424/24476保留。原apps/web不构建。
- 五项本地修复完成；等用户安排DeepSeek独立核查。不提交、推送或上线；旧发布包未操作。正式发布仍需完整提交此前与本次获准源码/测试，使用新SHA/新包，实体打印扫码仍待用户设备验收。
- 审计 `.audit/2026-09-20-final-p2-p3-fixes/`；只含本次增量的 current-task.patch、before原件、delivery-hashes、全套字面日志和浏览器/变异证据。工作记录归档，交接见 `docs/待核查-条码与看板P2P3-2026-09-20.md`。
