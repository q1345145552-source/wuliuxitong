# 移除员工端占位标签工具

## 授权与范围
- 用户在确认该工具只是占位实现后批准按建议移除。仅删 staff/page.tsx 的「标签打印系统」块和专用 labelDraft 状态；保留运单行正式打印、入库与标签工具分区/菜单、体积重量核算、装柜号和入库拍照。
- 不改正式标签格式/条码、写入规则、API、数据库、依赖或发布包。不提交、推送、上线。此前连续空格显示与看板写后刷新两个 P2 本次不处理。

## 进度
- 已读取根规范、Web 规范、记忆协议和最近交接；本地无 INDEX/ADR/glossary，依现有文档执行，不扩展脚手架。
- 已核全局引用：labelDraft 只用于待删块；openPrintLabel 的员工/管理员正式入口独立存在。
- 基线与原件保存在 `.audit/2026-09-20-remove-placeholder-label-tool/`；测试子代理仅负责增加测试，主代理删页面，独立代理复核范围。
- 已删除 37 行：4 行专用 state 和 33 行旧卡片（含空行）。独立复核按基线重建全文完全相同，其余业务源码哈希不变；旧输入拼接 document.write 的路径随占位块一起移除。
- 条码回归原 36 项逐字保留，新增 5 项 AST/真实两端打印回调检查：删除前 39/41，仅目标两项预期失败；删后 41/41。主代理最终重跑条码/身份/唛头/列表/详情/看板共 142 项全过；scripts/web 两套类型检查通过。
- 218 件 apps/packages 源文件与隔离副本一致，复用副本缓存重跑默认 Turbopack 构建 30/30。没有在原 apps/web 构建。
- 实际工具 section 提取 JSX + 合成空状态静态渲染，IAB 已看到仅三项其他工具，旧占位块消失，控制台 warning/error 为零；不是业务账号交互/E2E，也没有实体打印/扫码或生产验证。60472 临时服务与预览标签已关，原 3000/3001 PID 73424/24476 保留。

## 当前状态
- 本任务本地实现和验证完成，未提交、推送或上线；工作记录归档，报告在 `.audit/2026-09-20-remove-placeholder-label-tool/REPORT.md`。
- 连续空格文字显示与看板写后刷新两个 P2 仍未修复，其他 P3 也未顺带变更；不构成整批可上线结论。后续发布必须完整提交所有获准源码/测试新文件并使用新的目标 SHA/新包，旧包保持冻结。
