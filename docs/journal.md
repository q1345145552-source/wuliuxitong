# 项目交接日志

## 2026-09-11：删派送单不再抹掉已签收 + 运单列表品名搜到/导出全产品名（老板点的两件）

- **背景**：老板要求把 9-8「建派送单被拒只能先点签收」那条线索和「运单列表只显示第一个产品名」两件弄好。
- **查清的结论（只读查生产库，没有改数据）**：
  - 9-8 的根因（运单卡在旧 WD「派送中」，除了点签收没有别的出口）已经由 9-10 上线的「改派」解决：`/admin/lastmile/orders` 的 409 带改派提示，前端 `LastmileDispatchWorkspace` 弹确认后带 `moveFromDelivering` 重试。生产全表只有 15 次「已签收 → 又出车」：11 次的签收与重派间隔只有 0~13 分钟（就是当时那个手工绕法），1 次隔 101 分钟、1 次隔 1042 分钟（9-7 17:59 签、9-8 11:21 重派的 YW0001492-1），另有 2 次查不到在前的签收轨迹（7-03 YW0001276、7-10 YW0001244，都是早期孤儿单那批）。
  - 现存错状态 **6 行**（只读核对，未修）：① 运单在泰国仓却挂着「已签收」派送单 3 票 —— GZ260702122-1/WD000363、YW0001379-1/WD000359、GZ260800490-1/WD000476；② 运单已签收却还挂着「派送中」派送单 3 票 —— SZ260702524-1/WD000326、SZ260702590-1/WD000328、GZ260701854-1/WD000408（8-10、8-17 的存量，同票双派的闸是 8-25 才加的）。
  - 没有孤儿：子单里「已签收但无签收单」0 条、「派送中但一张单都没有」0 条；同时两张「派送中」0 票；1015 张已签收单**全部**有签收凭证图。
- **改的代码（两处，都不动存的数据）**：
  1. `admin-ops/routes.ts` 删除派送单：原来只查有没有别的「派送中」单，查不到就一律把运单退回「已到仓」—— 这正是上面①那 3 票的来路。现在多查一次「已签收」单：有就把运单保持/写回 `delivered` 并写一条说明轨迹（带两个 WD 号），父单照旧按全部子单推算，接口 message 讲清为什么没退回仓库。
  2. 运单列表品名 —— **故意不在后端改**：运单列表「详情」里的品名输入框绑的就是 `/staff/shipments` 回的 `itemName`（`buildShipmentOrderEditDraft` → `draft.itemName`），后端直接回「鞋 / 包 / 帽」的话员工一点保存就把这串字写回 `order.itemName`，真把数据改脏。所以只在页面上按产品行拼：员工端/管理端的**按品名搜索**和**Excel 导出品名**、客户端两张预报单表的**品名列和搜索**，统一走共享的 `productNamesLabel()`。编辑框继续绑原样的 `itemName`。
- **测试**：`test-lastmile-dispatch-rules` 加 23/23b（真路由 + 内存事务：有已签收单时保持已签收、没有时照旧退回已到仓）；把 23 的守卫关掉会红，证明能抓到。`test-lastmile-product-names` 加第 8 项：真 `GET /staff/shipments` 断言 `itemName` **仍是存的第一个产品名**、`products` 照旧下发，再加三页面的源码断言（含「编辑框必须继续绑原样 itemName」这条反向保险）。
- **本机验收**：改动前先取基线 —— 24 套测试 + `typecheck:scripts` + web/api 两套 tsc 全绿；改完再跑一遍，逐行与基线**完全一致**。另跑真 Turbopack 生产构建（`next build`）通过，为了不碰老板本地 `.next` 和在跑的 3000/3001，临时把产物指到 `.next-verify`，构建完已删目录并还原 `next.config.ts`；`next build` 顺手改动的 `next-env.d.ts` / `tsconfig.json` 也已还原。
- **没做的事**：没推送、没部署、没跑迁移、没改生产数据（上面那 6 行错状态一行没动，只是查出来报给老板）；没有在生产货票上实操签收/删除/改派。

## 2026-09-10：尾端品名补全 + 候选去空父单 + 改派三提交已安全部署（8d62e40）

- 老板授权后推送三提交 0b5a9f6、5f2ab4c、8d62e40；GitHub/服务器源码/运行API与Web均为8d62e40。按精确commit归档构建新镜像，服务器Turbopack 25/25，先API后Web；不运行旧deploy.sh，不迁移、不修数据，仅更新应用容器。
- 备份 `/root/deploy-backups/20260910T133414Z-lastmile-8d62e40`：库/图/配置本机另存双份，3份SHA256一致。真实隔离恢复45表、1409订单、2754运单、319图片记录；392个原图片文件备份及部署后hash一致。PG/Redis容器ID、启动时间、卷不变；.env、Compose和服务器原有package-lock差异保留。
- 补上之前没做的真实数据库验收：真路由 + 恢复库7项通过，包括双连接实际争锁、锁后签收历史/凭证、并发改派/签收、整车回滚与父子状态。线上只读验收，无真实货票改派/签收/删除。
- 线上API：1062派送记录品名；2170原候选/5页，排除1045空父单、保留1125票（含1087子单）；12个样本GET含30行导出/装柜和3个子单页签，非品名业务字段保持不变。真实三端桌面/手机6/6：卡片、全部候选、后续品名搜索、改派确认取消不发写请求，切换前验收会话继续有效。
- 独立数据核对：45表全部记录ID/数量相同，全部运单件数/体积/重量/状态不变；20条migration由备份pg_dump抽取与线上只读查询逐字段一致（最初空stdin比较无证明力，已作废并补真查）。
- 首次切API因验收脚本误读客户导出层级，自动回退旧API/Web实跑成功，未回退数据库。修正探针后先跑新镜像只读真生产路由，再重新切换成功；静态脚本复核+新旧镜像隔离演练均通过。测试库、测试卷/网、临时builder/会话凭证已清理，备份和旧镜像保留。
- 记录/精确源码包/patch/可运行回退：`.audit/2026-09-10/deploy-lastmile-8d62e40/verification.md`。默认rollback只回退应用像，不覆盖数据；隔离环境已清理，未来重新演练需重建。未宣称全系统无死锁/零停机；未做生产XLSX逐格视觉验收，导出接口实测且原有真Excel展开测试已过。
- 本条仅本地交接记录，未额外提交/推送；另一会话未跟踪 `docs/sessions/adversarial-review-20260908.md` 未动，本地3000/3001与原.next未操作。

## 2026-09-10：Claude 复核 Codex 的「候选去空父单 + 改派」——通过，三处小修后本地提交

- 独立复核（读全部 diff + 本机实跑）：锁序 [2901 → 旧派送单行(id 序) → 子单 → 父单] 与签收/删除的 [派送单 → 运单 → 父单] 对齐，锁后重读、已签收不删、同 WD 跳过、新冲突整车回滚都成立；22 项测试是真路由 + 真 service + 真工作台组件。本机：23 套全绿、三套 tsc 0 错。
- 三处小修：① 建单拦 0 件父单的判断改成 `(packageCount ?? 0) === 0`，跟前端候选过滤同口径（生产库只读核过：分柜父单 package_count 目前无 NULL，1293 张为 0——防以后）；② `hasChildren` 复用 `/staff/shipments` 里已有的「补父单总件数」那次子单查询，all=1 不再多查一次；③ 成功提示只在真的挪了/跳过了才带「改派 N 票 / 跳过 N 票」。测试加 3a（件数为 null 的父单同样拒绝），23/23。
- 变异重放（本机）：候选不过滤 / 去掉父单拦截 / 拦截退回 `=== 0` / 不删旧行 / 不重读 / 锁序反过来——全部红，恢复后字节一致。
- 未做：PostgreSQL 双连接并发实测、真实浏览器验收；部署另定。上线后要核：候选列表不再出现 0 件父单、勾到派送中的货会弹确认、确认后旧单里那票消失且轨迹有「改派」。

## 2026-09-10：尾端候选去空父单、派送中货物改派（仅本地，待独立复核）

- 基线 main `5f2ab4c`；保留之前两笔品名修复提交。仅改尾端接口/共用工作台，不提交、不推送、不部署、不连数据库、不起服务、不碰 3000/3001 或原仓库 `.next`；未查生产版本。
- `GET /staff/shipments` 仅 `all=1` 多一次本页父单子单存在性查询、增加 `hasChildren`。候选全页取完后只排除自己 0 件的分柜父单；留货父单、子单（含 0 件）、旧单保留，原件数/方数/重量及品名拼接不变。普通员工列表/装柜列表的字段和查询次数不变。
- 建单锁后拒绝 0 件分柜父单；没确认改派时 409 列全冲突单号和 WD。确认后同事务挪走旧 DELIVERING 行、建新行、运单派送中，只记一条「改派」轨迹。锁后已 SIGNED 的旧行和凭证保留；目标 WD 已有的票跳过（含已签收历史），返回 `moved` / `skipped`。请求 ids 去重、改派标志严格 boolean。
- 工作台管理员/员工共用：小字提示「在 WD… 派送中」；本地确认取消不发请求，服务端含「派送中」的 409 最多确认重试一次；成功提示改派/跳过数量，重复点击有 ref 防重入。
- 锁序：新建 2901 → 旧派送行按 id → 子单按 id → 全部相关父单按 id；追加无 2901。独立锁轨迹发现基线混选留货父单 zP 与另家子单 C 时会 zP→aQ，与柜子 aQ→zP 相反；本接口把直接祖先一并交原锁 helper，处理时过滤回选中 ids，祖先只锁不派。新出现、未预锁的 DELIVERING 冲突整车回滚，不倒序补拿派送行锁。签收/删除/共享 helper 均未改。
- 实跑：新增 `test:lastmile-dispatch-rules` **22/22**（真路由、真 service、离线真工作台 JSX/事件回调，严格投影桩、内存事务回滚）；全部 **23 套 test:* exit 0**，三套 tsc `--incremental false` 0 错；副本 webpack build **25/25**。第二双眼另核候选分页及签收/删除成对路径。
- 变异（副本）：M1 去过滤 `CHECKS 22; FAILURES 2`；M2 去空父单拦截 `FAILURES 2`；M3 不删旧行 `FAILURES 5`；M4 反锁序 `FAILURES 1`；M5 不重读 `FAILURES 1`；额外 M6 禁 409 重试 `FAILURES 1`、M7 跳过本地确认 `FAILURES 2`。七项均 exit 1，指定用例全部红，恢复字节一致后 `CHECKS 22; FAILURES 0`。
- 没做/没验：没有数据库双连接并发验证、真实浏览器页面验收或生产操作；锁环结论仅基于真实调用锁轨迹与代码分析，不宣称全库绝对无环。测试桩不是 PostgreSQL 的替代品。
- 原件、修改包、补丁、逐命令原样输出和仅源码回退：`/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-10/lastmile-dispatch/verification.md`。回退只在副本演练，不回退业务数据。当前结论：**可以交复核**（不是部署授权）。

## 2026-09-10：尾端派送与派送单「品类不全」——多产品运单只带出第一个产品名（仅本地）

- 老板反馈：尾端派送页面信息好像不全（不确定），导出的派送单肯定「品类不全」。生产日志只读核对：最近 10 天整柜拆柜清单导出 14 次、客户签收单 3 次；无 5xx。
- **病根**：员工建单 / 客户预报单在有产品行时，`order.itemName` / `shipment.itemName` 只存**第一个产品名**（orders/routes.ts `products[0].itemName` / `staffProducts[0]?.itemName`；批量导入是拼接的所以没事）。尾端派送卡片、客户签收单（分柜单不展开产品行那条路）、整柜拆柜派送清单（products 故意为空）都拿这个字段当整票品名。
- **修法（只改显示，不动存的数据）**：新增共享 `packages/shared-types/product-names.ts` `productNamesLabel()`（按 sortOrder 去重拼「 / 」，跟批量导入一致，无产品行退回原 itemName）。改四处：`GET /admin/lastmile/orders`（卡片）、`GET /admin/lastmile/customer-export-data`（签收单）、`GET /staff/loading-manifests/export-data`（整柜清单，`loadOrderProductDims` 顺带带出 `names`）、前端 `fetchLastmileShipments`（可派送候选列表）。
- 新增 `scripts/test-lastmile-product-names.ts`（7 项，真调三个路由 + 走到 Excel 行，Prisma 内存桩），接 npm/CI。变异自证：三处各退回老写法 → 对应用例红；恢复后字节一致、全绿。三套 tsc 0 错，全套自测全绿。
- **没改**（同一个字段、同样只显示第一个产品名，报给老板决定）：装柜明细 `loading-manifests:227`、柜子接口 `containers/routes.ts:186/1043`、员工/管理员列表接口 `shipments:497` `admin:491`（列表本身另有产品行明细，不影响）。建单存 itemName 的口径也没动。
- 另一条日志线索（未处理）：9-8 上午员工 5 次建派送单被 409 拒（运单还挂在旧 WD「派送中」），之后逐票点签收才建成——「签收」会把运单写成已签收；改派/二次派送的正规路径是删旧单那票。未确认是否老板说的「不对劲」。
- **Codex 第一轮复核**（`docs/codex-复核-尾端派送品类不全-2026-09-10.md`）：诊断方向对但不全——运单 `Shipment.create` 根本不写 itemName（可能为空），管理员编辑也传首产品名；四处修复 ✅。报 **P1**：卡片「物流轨迹」弹窗的**子单页签**仍只显示首名或「—」（`containers/routes.ts` track 接口 `children[].itemName = cs.itemName`）；**P2**：新测试的桩无视 select、且没测前端候选链路，M6/M7/M8 三个变异假绿；**P3**：拼长的品名在候选/卡片会被省略号截掉且无 title。
- **按 Codex 三条整改**：① track 接口 `children[].itemName` 改用 `productNamesLabel(shipment.order?.products, cs.itemName)`；② 测试重写——Prisma 桩严格按 select/include/orderBy/take 裁剪、未实现的模型/方法即抛，新增第 5 项（真前端 `fetchLastmileShipments` → 真 `GET /staff/shipments`，501 票两页）和第 6 项（真 track 路由两个子单）；③ 两处品名 span 加 `title`。9 个变异（Codex 的 8 个 + 轨迹子单退回）全红、恢复后字节一致、9/9 绿；三套 tsc 0 错、全套自测全绿。第二轮提示词已追加在同一文件。
- **Codex 第二轮**：P1 ✅、P3 ✅、P2 ⚠️ 剩两个测试缺口——桩的 include 分支 `{...row}` 会把没点名的关系漏出去（查询改 `include: {}` 仍绿）；第 2 项只看响应、看不出查询多选了 `signImageBase64`。**已补**：include 语义改成「标量全给、关系只给点名的」；桩记录查询参数，第 2 项直接断言 select 内容。11 个变异（M1–M9 + 这两条）重放全红、恢复后字节一致；业务代码这轮未动。第三轮提示词已追加。
- **老板追加（9-10）**：装柜管理柜内货物那一行的品名也拼全——`GET /staff/loading-manifests/detail` 的 `order.select` 多带 `products{itemName,sortOrder}`，`bills[].itemName` 用 `productNamesLabel`。测试加第 7 项（真路由，两票：多产品 / 老单），退回老写法即红；tsc 0 错。单独一个本地提交。**未动**：`containers/routes.ts:186`（`/admin/containers/detail`，Codex 查无前端调用方）、列表接口 `shipments:497` / `admin:491`（列表另有产品行明细）。
- **Codex 第三轮：「可以提交」**。两条 P3 建议（关系写 `true` 时桩只给标量；「有没有图」那次查询断言只选 id）已顺手补上，9/9 绿、抽查 M6/M11 仍红。按老规矩做**本地提交**（一个提交，含三轮整改），未推送、未部署。上线后要专门核：尾端派送卡片品名、候选列表品名、轨迹弹窗子单页签、两种派送单导出的品名列——这次动了 4 个接口的返回内容（列表 / 客户单 / 整柜清单 / 轨迹）。

## 2026-09-07：49126aa 三端数字列对齐已推送部署（只更新 Web）

- 老板明确要求“部署”；7文件hash与上轮最终验证一致，提交/推送 `49126aaa58f39ebd818e6dcd747d6b5d364f9a0a`。服务器HEAD和运行Web revision一致；只更新Web，API仍保留88918ac同一容器，PostgreSQL/Redis及挂载不动，无迁移/db push/业务数据写入。
- 本次21套非数据库自测、scripts/API/Web三套类型全绿；服务器从精确commit独立归档构建Web，25/25页面通过；候选四入口200，旧Web隔离启动回退演练及本地回退入口实跑通过。本地3000/3001仍原PID67825/32916，未在原仓库build。
- 新备份 `/root/deploy-backups/20260907T025822Z-alignment`：真实隔离恢复45表、1323订单、2557运单、318图片记录；391物理图片逐一hash。数据库/图片/配置另存本机`.audit/2026-09-07/deploy-alignment/data-backup/`，三份SHA256一致。
- 10:04:00切Web，10:04:33健康/数据核验通过（Asia/Bangkok）。切换前后45表记录数/主键集合一致，391原图片hash一致、迁移账本相同；API/PG/Redis ID/启动时间/挂载及服务器原package-lock差异、.env、compose保留。
- 真生产浏览器：基线三端桌面3/3；部署后三端×1440/390最终6/6。管理员/员工各50行、客户14行，体积/整票总重量/总箱数逐票保持，3/2位不变；54个采样数值与表头文字右缘最大偏差0px，勾选/横滚后仍一致。详情/状态列/侧栏手机抽屉原功能一并通过，三端数字局部截图已目检。
- 初跑5/6，原因是客户一票GZ260801786在切换后10:04:51发生员工子单签收→父单同步（独立只读SQL核实子单轨迹outForDelivery→delivered）。不是排版错误；不回退正常业务，仅对该票该次状态转换作精确预期调整，数字及其余字段仍严格比较后6/6。保留初次exit1和调查证据，不伪报首次全绿。
- 正式公网四入口200，CSS字节与运行镜像一致；客户异常2=独立计算2、total14保持。临时会话/容器已清理，不改老板浏览器登录。只读验收，未测试生产新增/编辑/导入等写入；不会将切换时计数一致描述成后续没有正常业务更新。
- 修改源码包、完整提交patch、命令原样记录、已演练的仅Web回退：`.audit/2026-09-07/deploy-alignment/verification.md`。代码已推送部署；本条部署交接仅本地追加，未为日志再触发一次上线。

## 2026-09-07：三端运单数字列统一对齐（仅本地，未再次部署）

- 老板指出截图数字列歪，批准三端一起对齐。基线f801370；保留上一轮已部署交接日志，没有延用上轮授权再次上线。
- 实测列边界本身一致，偏差在文字：管理员/员工体积和重量的th右边距6px、td10px，文字右缘差4px；客户端总箱数表头靠左、数值靠右，样本差32px。客户端当前体积/重量本来已同右缘，不误报为全部列宽错位。
- 三端总箱数th/td补统一shipment-metric；该局部数字列统一右对齐、左右8px、正常字距；体积/重量列表头单位使用半角括号，消除全角括号尾部视觉空白，含义不变。未动字号、列宽/顺序、状态、分页、产品内容、输入/导出/接口/数据库，体积3位、重量2位及整票合计公式不变。
- 真浏览器合成接口覆盖三端1440/390、每端50条可见行：基线18通过6失败→修改/独立生产包24/24；所有原数据行文字/列宽/列数保留（表头单位括号仅作排版标准化），54个采样数值单元格与对应表头文字右缘误差<1px，管理员/员工勾选、横向滚动后也不偏。基线逐变量探针证实仅改对齐仍有边距差，同时统一才归零。
- 扩充test:shipment-list-display为11项：从三端真实页面AST抽出数字表头、单元格及客户端派生变量，实际React渲染验证标记、0值、3/2位和整票优先级。新测试基线红，客户端夹具漏派生绑定已补齐，不计为业务故障。去掉客户端总箱数标记→红；撤销CSS统一边距→浏览器红；恢复后源码字节一致。
- 列表11、详情15、前端13、产品25、状态19通过；scripts/Web类型0错；独立副本webpack构建25页并复跑浏览器24/24。未重跑全部21套/API类型，不混用部署前旧记录；未连真实数据库、生产或重启本地3000/3001。
- 原件、7文件修改包/补丁、逐条命令和输出、防覆盖回退副本实跑在`.audit/2026-09-07/metric-alignment/verification.md`。仅同步本地4个页面/样式文件+1测试+2记忆文件；本轮无提交、推送、部署。

## 2026-09-07：f801370 已推送并安全部署（仅更新 Web）

- 老板明确授权再次核对后直接推送部署、保护数据。本地/GitHub/main、服务器HEAD和运行Web镜像revision均为f801370；API源码未变，保留原88918ac镜像及同一容器，不将其旧revision误报为漏部署。
- 最终提交12文件：列表状态列、管理员详情补齐/详情柜号去除、侧栏深蓝灰、状态换行/总箱数名称与回归。重量标签及公式保持，三端外层仍为整票总重。21套非数据库测试、scripts/API/Web三套类型全部通过；服务器从精确commit独立归档构建Web，25页通过；原本地3000/3001未动。
- 备份在`/root/deploy-backups/20260907T012648Z-ui/`：数据库真实隔离恢复成功（45表、1294订单、2527运单、318图片记录）；391个图片文件归档逐一校验。DB/图片/配置归档另存本机`.audit/2026-09-07/deploy-ui/data-backup/`，三份SHA256一致。无迁移、未执行db push，不恢复/覆盖生产数据。
- 08:35:36开始切Web，08:36:09健康/数据核验完成（Asia/Bangkok）。只执行Web的`up -d --no-deps --no-build`；API、PostgreSQL、Redis容器ID/启动时间/挂载未变，服务器既有package-lock差异、.env和compose保持。45表记录数与主键集合未变；391个原图片hash一致，迁移账本未变。
- 真实生产浏览器：切换前3角色各桌面基线；切后3角色×桌面/手机6/6。管理员/员工各50条、客户14条可见行逐票体积/总重与基线一致，状态列紧随运单号、对齐正确，管理员详情有内容/总箱数/原重量标签且无柜号，员工详情无柜号，手机260px导航同色且可关闭。全部业务请求限GET/HEAD/OPTIONS，未测试生产新增、编辑、导入/上传等写入流程；未把模拟写入回归当生产写入验收。
- 公网login/admin/staff/client均200；CSS字节hash与运行镜像一致。客户端异常实际2=独立计算2，total仍14、阶段未改。切换前签发的短期会话切后仍有效；验证结束清理临时凭证、API临时文件和隔离容器，没有放进老板浏览器。
- 旧Web镜像/仅Web回退脚本已保留并做隔离镜像实跑，未在生产执行回退；新源码包+完整patch在副本331文件逐一对齐。证据`.audit/2026-09-07/deploy-ui/verification.md`。本条交接日志仅本地追加，业务代码均已提交推送部署。

## 2026-09-07：按老板只执行复核项1、3，重量保持（仅本地）

- 已收到并读取Claude正式报告，报告结论为可进入部署准备，非部署授权；其判断对应88918ac→冻结源码。老板随后只批准1状态换行、3统一总箱数；2产品重量标签暂不改，空行也未额外处理。
- 三端状态列仅将white-space: normal提优先级以覆盖共用td内联nowrap；管理员详情“总件数”改“总箱数”。生产源码严格只有这两处，不改字号/列宽/数量或重量公式、标签、API、数据与导出。
- 重量口径已追源：三端外层列表和管理员详情顶部用totalWeightOf整票总重；管理员详情产品行直接取product.weightKg，每箱重量在建单/批量导入乘箱数后合计。解释标签建议不等于改数据，更不等于把外层总重改单箱；本次全部重量原样保留。
- 真浏览器合成接口：基线12通过8失败→修改20/20；覆盖三端桌面/手机，22px压力字号长状态完整分行无裁切、总箱数与值、列表/顶部总重和产品重量分别独立取值保持；生产构建后同样20/20。截图22px是测试注入，不是源码字号变化。
- 新增实际详情JSX渲染断言锁定“总箱数”且重量标签/值不变，基线红→15/15；既有列表8/8、前端防线13项、scripts/Web类型及独立副本25页构建通过。未重跑全部21套/接口类型，不混用历史结果。
- `.audit/2026-09-07/review-touchups-1-3/verification.md`留存原件/修改包/补丁/精确输出/防覆盖回退副本实跑。只同步本地4文件（含测试与journal），保留其余未提交改动；未提交/推送/部署或操作真实数据，原3000/3001未重启。

## 2026-09-07：准备交给Claude的待部署复核材料

- 老板要求Claude检查本地改动是否有bug、是否能部署。固定88918ac→当前工作区，冻结11文件（含3个新文件）与hash/完整patch，范围含状态列、详情修复、侧栏配色。
- 当前终端Claude Code 2.1.158的auth status为loggedIn=false/authMethod=none，Claude独立审查尚未开始；不能把材料准备或此前自测当成Claude绿灯。
- 提示词：`docs/claude-复核-2026-09-07本地待部署改动.md`；冻结证据：`.audit/2026-09-07/claude-review-request/`。本次只读核git差异、diff --check、API/迁移/锁文件未改，未复跑业务测试、未查生产。
- 部署状态：待Claude独立复核。未改业务代码、提交/推送/部署。提示词要求最终完整副本构建及明确已测/未测边界。

## 2026-09-07：功能栏深蓝灰配色试改（仅本地）

- 老板要求本地改功能栏颜色供查看。三端共用侧栏试低饱和深蓝灰#24343b，选中#3b5560/浅蓝灰细线，浅灰文字；主页暖白不变，不加渐变/图标/新布局。
- 仅编辑ledger.css既有侧栏颜色和局部变量，菜单链接、宽度、字体、折叠/抽屉、会话、列表/详情及原未提交修复保持；新配色只待老板视觉反馈，不据此改长期业务约定。
- 真浏览器：在上一轮独立生产构建上加载完整候选CSS，三端桌面/手机6组全部通过：前后几何/主页背景/表头/链接相同，文字对比度≥4.5，选中/hover/焦点/退出提示、分组折叠、侧栏收展、手机ESC/关闭、撤去候选样式回原色且会话保留。接口全部合成拦截。源码CSS解析/差异确认仅侧栏颜色变化；未重跑上一轮21套测试和构建，不把旧结果当新跑。
- 初版预览只注入侧栏基础块，顺序压过手机媒体规则；已改为完整候选CSS，保留260px抽屉。颜色断言等待原有过渡结束后判定；这些是夹具校正，实际源码的尺寸/过渡没改。
- 原件、修改包、补丁、原样命令、回退副本实跑：`.audit/2026-09-07/sidebar-colour/verification.md`。只同步本地，未提交/推送/部署或操作数据；原3000/3001未重启。回退仅撤本次配色，不撤运单状态和详情修复。

## 2026-09-07：修复管理员运单详情缺内容，移除详情柜号（仅本地）

- 老板确认管理员截图问题并同意修复；浏览器用完整合成响应复现：管理员“详情”原本仅渲染摘要和图片区，没有产品/状态/合计正文。员工、客户已有详情，非同类空白。未连真实库，不从该复现推断生产数据情况。
- 管理员挂载只读详情组件，显示唛头、当前物流状态、仓库、运输方式、日期、数量、整票体积/重量、地址/备注和完整产品表；体积3位/重量2位、产品单箱数量保留在详情，老单缺产品行时回退原字段，空文本显示“—”。未新增普通运单金额。
- 管理员详情柜号整项移除；员工详情摘要及只读“柜号/装柜号”共3处一并去掉；客户端本来不显示。创建/编辑、装柜管理、关联字段、API/数据库/导出不动，之前未提交的主列表状态列完整保留。
- 新增14项真实页面详情JSX渲染回归并接npm/CI；基线失败→14全绿，4种变异（删挂载/加柜号/截产品/剩余重量充合计）均被捕获。修复前详情30项12通过18失败→30/30；三端桌面/手机原列表62/62，8次XLSX下载值与此前基线一致；两种屏宽实测图片模拟上传/删除、Tab焦点/ESC关闭。独立生产构建后的浏览器同样通过，不用dev结果代替构建验收。
- 21套非数据库测试、scripts/API/Web三套类型、Next25页生产构建通过。原3000/3001未重启，全部业务接口/图片写入均被测试夹具拦截；未操作真实数据、提交/推送/部署。详情内容覆盖此前管理员只验编辑框、漏验只读正文的缺口。
- 原件/修改包/差异/逐条命令输出/回退副本实跑见`.audit/2026-09-07/shipment-detail-repair/verification.md`；回退只撤本轮，保留上一轮状态列及部署交接记录。生产真实账户仍未验收本次本地修复。

## 2026-09-07：主列表直接显示物流状态（仅本地）

- 老板确认：最外层单箱数量列让位给物流状态，并提前显示。三端均将状态放在运单号后；管理员/员工外层产品列省略单箱数量，客户原状态列仅前移。其余现有列、取值、总箱数、3/2位精度与轨迹按钮保留。
- 仅列表投影移除单箱数量；共用完整六列产品明细仍保留，员工详情和管理员编辑输入均实测保留“个/箱”。输入/提交/导出、API、数据库、海陆状态流与客户隐私口径不改；导航配色未改。
- 历史核实：6月5日de33e5c/5b4fb1b分别在员工/管理员表格重排中移除状态；6月6日4286b9d才把品名拆出单箱数量等独立列。不是9月6日提交将两列直接替换，但此前复核只对近期基线，漏了历史状态缺项。历史字段清单见本轮history.json；已退役金额/柜号等列不因追溯而恢复。
- 验证：新版位置基线0/6→修改6/6；三端桌面/手机62项浏览器检查通过（合成API），逐票核对原列值、海陆/空/旧状态、43项产品滚动、详情数量、8次真实XLSX下载（全部/勾选内容与基线一致）。两种变异“状态写死/数量错占尺寸列”均被抓住，恢复后复验通过。新增8项真实组件渲染回归接npm/CI；20套非数据库自测、3套类型及25页生产构建通过。
- 原件/修改包/差异/精确命令日志/保守回退见`.audit/2026-09-07/inline-shipment-status/verification.md`。保留之前本地journal部署记录；未提交、推送、部署或操作真实数据，原3000/3001服务未重启。记录浏览器验证不等于生产真实账户验收。

## 2026-09-06：88918ac 已获授权推送并安全部署

- 老板明确要求上线且保护数据。GitHub/main、服务器HEAD和运行Web/API镜像revision均为88918ac；旧服务器为4691a4e。12:08:07开始切换，12:08:42健康/数据校验完成（Asia/Bangkok）。实际公网入口为https://xianlianth.com，API为https://api.xianlianth.com；按nginx upstream核实，未改其他站点、nginx或DNS。
- 无迁移。先做数据库/图片/配置备份，隔离PostgreSQL真实恢复成功（45表、1294订单、2527运单、318图片记录），391个物理图片文件归档逐一校验；数据库/图片备份另复制本机且hash一致。服务器备份与应用回退脚本在`/root/deploy-backups/20260906T045808Z-88918ac/`。
- 从目标commit干净归档构建（Next25页通过），旧服务在构建期间持续运行；仅`up -d --no-deps --no-build api web`。数据库/Redis容器ID、启动时间及卷未变，图片挂载未变；45表计数/有id表的主键集合无变化，391旧图片hash一致。未写业务数据或改表结构。服务器原package-lock.json本地差异、.env和compose文件均完整保留。
- 独立验证：三端真实认证GET及正式域名rewrite链路正常，旧版签发的短期令牌在新版仍有效；客户14单中attention应为2/实为2，正常单排除、阶段不变、total保留14。公网四入口200、TLS正常，新CSS与运行容器hash相同。不是完整浏览器/写入流程验收，也不冒充生产跨页测试。
- 旧应用镜像和只切镜像的回退脚本保留；已检查脚本/镜像/配置，未在生产实际回退。短期校验令牌文件已清理。登录页不清已有会话、双层主题保持批准版本，不额外改动。
- 详细证据与本机备份：`.audit/2026-09-06/deploy-88918ac/verification.md`及`data-backup/`（忽略入Git）。本交接追加仅本地文档，未另提交/推送；未改变已上线业务代码。

## 2026-09-06：Codex 独立复核 9-05 改动 + 8 处修复，报 2 P2 + 3 P3，全部处理（本地）

- Codex 结论「先修再提交」，未发现 P1。销号表：8 处修复 5 个 ✅、3 个 ⚠️（功能对但反馈/测试保护不全）。提示词在 `docs/codex-复核-9-05全天改动与8处修复-2026-09-05.md`。
- **P2-1 已修**：管理员 `loadOrders` 现在返回 `applied | stale`；被自动刷新作废的手动刷新不再弹「已刷新」（改弹「列表已由自动刷新更新」），作废请求的失败也不再冒充新结果的失败（catch 里先验号）。
- **P2-2 已修**：`test-session-api` 的裸 fetch 扫描从正则改成 TypeScript AST：认 `parseApiResponse<Array<T>>(`、`globalThis/window/self.fetch(`，忽略注释和字符串；显式传了 `sentToken` 的调用不算违规。带 7 个人造源码的正反例自证（Codex 列的漏报/误报清单逐条覆盖）。
- **P2-3 已修**：`test-attention-filter` 新增真路由用例——前端服务的 fetch 直接打真的 `GET /client/orders` 处理函数（Prisma 换成认 skip/take 的内存桩），620 单、异常单分散两页 3/5，断言每页只回异常单、total 仍是筛前 620、阶段字段没被改写。**变异自证实跑**：把后端 `wantAttention` 分支禁掉 → `FAILURES 1`（回 [500,120]）；恢复后字节一致、全绿。
- **P3 已修**：员工端导出提示改「仅导出当前结果中的已选运单」；登录页那条与 trim 矛盾的旧注释改掉；`.section-divider` / `.section-label*` 五条死样式删除。
- **Codex 第二轮**（实测重放）：P2-2/P2-3/P3 全 ✅；P2-1 仍 ⚠️——stale 分支提示「列表已由自动刷新更新」在轮询随后失败时仍是虚报。**已改**为「本次刷新已被较新的请求替代」（只说被替代，不替后一次请求宣布结果）。它另指出扫描漏 `const f = fetch; f()` 和 `window['fetch']`：扫描改成按**引用**算裸 fetch（含 globalThis/window/self 的 `.fetch` / `['fetch']`），自证用例 7 → 12 条。第三轮提示词已追加在同一文件。
- 终态：三套 tsc 0 错、19 套自测全绿（test-attention-filter 9 项、test-session-api 27 项）。Codex 那边 `test:ai-timeout` 是它沙箱不许起监听器被拦，不是项目问题。仍未跑 next build（Codex 在副本里跑过 25/25）。
- 待老板：登录页清不清上一个人会话的选边；两层主题（a3/ledger）留待有真数据时做。只改本地，未提交、未推送、未部署。

## 2026-09-05：全天未提交改动的复查与修复（本地）

- 老板要求检查今天 12 轮改动有没有新 bug。基线实跑：三套 tsc 0 错、19 套自测全绿。8 路并行审查（逐行/删行/跨文件/复用/简化/效率/深度/规矩）+ 逐条核实，报 10 条，修 8 条，1 条待老板拍板，1 条留待视觉验证。
- 已修：① 登录页恢复 `password.trim()`（管理员设密码路由存的是 trim 过的哈希，不 trim 客户复制唛头带空格就登不进）；② 员工端列表分页加 totalPages 回夹（跟管理员端同一道）；③ 员工端「已选 N 条」/导出按钮/全选框按当前结果计数（镜像管理员端 selectedResultOrders）；④ `shipment-search-reset` 禁用态、`shipment-search-hint`、`auth-field-hint` 补 CSS；⑤ 管理员 loadOrders 加 request-gate，手动刷新与 10 秒轮询不再乱序覆盖；⑥ **「异常」分组后端也认 `statusGroup=attention`**（orders/routes.ts 用共享 matchesShipmentListFilter），客户端带参数、前端筛保留兜底——老后端不认参数时结果照样对；⑦ 批量导入读文件的手写序号换成 `createRequestGate()`，门闩新增 `cancel()`；⑧ core-api 401 对「没经 fetchWithSession 打标的响应」加开发期告警，test-session-api 新增源码扫描（调 parseApiResponse 的文件必须 `fetchWithSession as fetch`，带变异自证）。
- 新增/改动测试：test-attention-filter 8 项（新加「新后端服务端筛过也一致且参数真的带了」）、test-client-status-group 19 项（新加 4b attention 只放异常单且不改阶段）、test-session-api 27 项。修后三套 tsc 0 错、19 套全绿。**没跑 next build**（3000 是老板在用的 dev 服务，build 会砸 .next）。
- **待老板拍板**：`prepareLoginPage` 现在有会话就不清（修双标签误退出），反转了 8-31 排查报告第 57 条「打开登录页就清上一个人的会话」的共用电脑防线。两边都有理，未动。
- **留待视觉验证**：RoleShell 的 `variant` 已是死参数，`.a3-shell`（258 条）与 `.ledger-shell`（66 个 !important）两层主题同时生效靠 import 顺序压住。删 a3 层需要真数据截图对照，本轮没条件（见下）。
- 复查时发现：Neon 测试库当晚连不上（TCP 通、Prisma P1001，本地 3001 假账号登录返回 500 而非 401）——本地登录/真数据浏览器验证都做不了，只用假会话验证了登录页大写锁定提示样式。生产未查。
- 只改本地，未提交、未推送、未部署、未动数据。

## 2026-09-05：晚间交接更新（仅文档）

- 已按当前 Git 与审计整理最新交接：main/本地 origin/main 引用仍为 4691a4e，保留全天未提交 UI/会话/模板及边界修复；未 fetch，不推断远端实时状态或线上版本。
- 区分上一轮 19 套全量/三套类型与最新 24 项读取边界/Web类型/构建，补充异常关注维度、3/2 位显示、模板差异与阶段回滚限制。此次只核对记录及源码哈希，没有重跑业务测试、操作服务或数据库。
- 交接文件：`/var/folders/hf/lsm4_4593slb8wrxnvnpxp180000gn/T/xiangtai-handoff-20260905-eat4sx1y/湘泰物流系统-交接文档-2026-09-05.md`。按 handoff 约定放系统临时目录；转交需另存。09-03 文档保留历史，不覆盖其当时记录。

## 2026-09-05：批量文件读取边界复核（本地）

- 继续检查上一轮未覆盖的文件读取边界，复现两类既有问题：管理员/员工慢文件可覆盖新文件、关闭后仍回填；管理员读取失败还保留上份预览并抛出未处理异步错误。员工原有失败清理已正常。不是本轮模板下载代码引入。
- 两端加读取序号，换文件/关闭/卸载使旧读取失效；读取开始清空旧行与确认状态，读取中仅显示进度、不显示确认入口。管理员补失败与空文件提示；员工保留原大小/行数/字段解析防线，旧失败不清掉新结果。提交循环、字段映射、API服务不改（AST/字节对照）。
- 基线6场景1通过5失败→修复6通过；扩展两端桌面/手机24场景通过，涵盖换文件、关闭重开、失败、读取中、迟到失败、空文件。关闭失效/过期判断两种变异分别4失败，均准确抓到旧文件完成；恢复后24通过。模板下载4项、管理员完整模拟提交2项、三端功能46项通过；批量解析100单/300明细、前端防线、产品行、Web类型与25页构建通过。
- 仅本地，不动真实业务库/生产/推送部署。记录/快照/补丁/防覆盖回滚见`.audit/2026-09-05/ui-edge-check/`。上一轮19套全量与API/scripts类型记录仍有效但本轮未重复宣称新跑；本轮聚焦读取边界。

## 2026-09-05：UI全量回归与导出反馈修复（本地）

- 用户要求检查前几轮UI/模板变更。发现一项新增交互缺陷：导出移入原生dialog后，空日期结果与下载失败仍写在弹窗外，管理员/员工均复现“点击无反馈”。新增专用导出反馈状态，在弹窗内显示空结果、成功或失败；改日期/重开/重试清旧提示，不改取数、导出文件和防重逻辑。
- 新回归：原版两端0/2→修复版桌面/手机4/4，覆盖空日期、Blob URL失败注入、重试下载、状态清理。全功能46、列表/导出30、数字精度36、两端模板4、管理员完整模拟导入2项通过（合成海/陆各一行、前导零、失败行、不重复提交已完成批次）。导出函数AST移除新增反馈写入后与原版一致。
- 当前冻结副本19套非数据库测试全通过，scripts/API/Web三套类型检查通过，Next webpack生产构建25/25页通过，含登录会话、状态、金额、AI两时区。未访问生产和真实数据库、未推送部署；模拟POST全拦截，无真实导入。
- 审计原件、修改包、补丁、复现、失败注入截图、命令原样输出与防覆盖回滚：`.audit/2026-09-05/ui-regression-check/`。这轮未发现其他已确认的新增故障；测试通过不等于线上或真实数据验收。

## 2026-09-05：补齐管理员批量导入模板入口（本地）

- 用户报告找不到下载模板。浏览器复现管理员桌面/手机均无按钮，员工端下载正常；只读HEAD和上一轮基线也无管理员模板按钮，上一轮UI补丁未删除模板代码。管理员此前只有“下载模板”说明，界面验收漏项。
- 管理员批量导入弹窗补“下载模板”，生成11列匹配现有导入字段的XLSX及填写说明，不复用不兼容的员工多产品模板；不预置示例订单，明确日期/编号先设文本格式。下载失败提示、下载中禁用。
- 真实浏览器下载并读回工作簿、填入合成数据再上传预览（含前导零）：两端桌面/手机4项通过。三端原控件/回调AST一致，仅新增下载按钮；员工页面与上传/导入映射未修改。Web类型和25页构建通过；没有实际提交业务导入或访问数据库/生产。
- 初稿格式化空字符串行被XLSX视为可导入数据，回归捕获后移除，空模板解析0行。副本补丁/回滚和原件见`.audit/2026-09-05/batch-template/`，仅本地，不推送部署。

## 2026-09-05：运单列表操作与扫读（本地试改）

- 用户同意先试顶部收紧、固定识别列和数字对齐。管理员/员工导出入口移到结果栏，日期条件进入原生dialog小弹层，原导出函数、日期/勾选口径、禁用状态不改；关闭/ESC回焦、Tab循环与日期保留已验证。
- 三端体积/重量右对齐、等宽数字、表头带单位，保留3/2位。桌面固定勾选（如有）、唛头、运单号；小于1024px只固定运单号，避免遮满手机。仅外层身份单元格加类，产品内表/展开明细不受影响，原浅灰横线不改。
- 副本浏览器30项通过，包含三端桌面/手机滚动、弹窗键盘与Excel全部55条/选中1条/日期1条；原功能46项、数字精度36项通过；三端现有输入/回调/effect AST对照一致。Web类型检查及25页生产构建通过。初稿Tab边界及客户端多加数字类已修正，失败记录保留。
- 仅本地试改，无推送部署、真实业务库或生产访问。审计/截图/修改包/补丁/防覆盖回滚见`.audit/2026-09-05/list-efficiency/`；浏览器业务数据全部为合成拦截。

## 2026-09-05：体积/重量显示精度（本地）

- 三端运单列表统一体积3位、重量2位，复用既有formatMetric；同步客户产品重量详情及创建预览、员工预览/预报只读展示、管理员计算器估算重量。空值和聚合值取值优先级不改；已有编辑草稿的零值语义不改。
- 仅展示舍入/补零，不改输入、提交、计算函数、API、数据库和Excel导出原始数值。浏览器合成数据：桌面/手机三端36项精度通过，原基线16通过/20不满足新口径；功能回归46项通过，客户预报实际显示0.155但拦截提交仍是0.155062。Web类型检查和生产构建通过。
- 备份、补丁、验证日志、防覆盖回滚：`.audit/2026-09-05/measurement-precision/`。只本地变更，无推送部署、生产访问或数据库操作。

## 2026-09-05：运单浅灰分界线（本地）

- 用户要运单边界略明显但不深、不花。仅追加 ledger.css：外层整票运单统一2px浅灰 #dce1d9，产品内部1px更淡 #eff1ed，末产品行不画悬空线。没有新增竖线、斑马纹或装饰，不改背景/字段/筛选逻辑。
- 在独立浏览器上下文注入同一CSS副本验证：三端桌面/手机25项通过，覆盖整行连续边界、内外线层级、列宽、产品24px行高与72px视窗、勾选、长产品滚动；删除边界变异被捕获。基线12项视觉目标未满足（预期）。不改业务代码，未重跑类型/构建/后端测试；业务请求均为合成拦截。
- 原件、修改包、补丁、验证与防覆盖回滚：`.audit/2026-09-05/shipment-row-lines/`。只本地样式调整，不推送部署或改数据。

## 2026-09-05：异常筛选替代退回/取消入口（本地）

- 老板确认：异常包含延迟发出、运输延迟、口岸滞留、封港、三地海关查验、明确异常；正常卸柜/预约派送/派送中排除。六按钮最后一项改为“异常”，保留紧凑单行。异常是关注维度，与在途可重叠，不改阶段统计或状态机；历史退回/取消仍留在全部订单，不删除数据。
- 共享 matchesShipmentListFilter 复用 ATTENTION_STATUSES。管理员/员工直接筛已加载列表；客户端 fetchClientOrders 先按 total 取完全部页再筛当前状态，不发旧API不认识的新参数，三条查询/轮询入口与原请求门闩继续共用。API、认证和数据库未改，不需迁移或重启API。
- 新增真实共享函数/客户端服务7项回归，620条跨页与第二页失败、异常解除、正常排除及缺项/误收变异均覆盖，基线2绿5红→7全绿；加入npm/CI。三端模拟接口浏览器23项（含分类导出、异常解除）及原功能46项通过；19套非数据库回归、3套类型检查和生产构建通过。浏览器数据均合成，不等于真实账号或线上验收。
- 修改/原件/补丁/保守回滚与完整命令记录位于 `.audit/2026-09-05/attention-filter/`；仅本地同步，不推送部署或读写业务库。操作文档同步说明异常范围、阶段交叉和旧记录保留。

## 2026-09-05：运单筛选收为单行（本地）

- 用户纠正上一轮分类卡片太大：运单列表为主体，删除“运单分类”标题、当前分类说明和六项释义。共用单行文字筛选，桌面40px、手机44px；窄屏横向滚动而不是换行，占位从桌面116px降至41px。选中项保留浅底、加粗和深色下划线。
- 仅调整 ShipmentStatusGroups 与 ledger.css；三端页面筛选/分页/导出、认证、API、共享状态定义均字节未改。浏览器发现390px键盘聚焦末项仍部分被遮，增加就近滚入可视区后复验通过。
- 三端5宽度单行/焦点/会话30项、原功能46项及Web类型检查通过（合成API，不是真实账户验收）。本轮未重跑全套构建或数据库测试；补丁、原件、精确验证与保守回滚见 `.audit/2026-09-05/compact-status-tabs/`。只本地修改，不推送部署。

## 2026-09-05：运单分类视觉强化（本地）

- 延续文字优先方案：管理员、员工、客户共用六类分栏，16px 分类名、简短业务释义、深墨色选中态；桌面一排六类，窄屏三列、手机两列。无表情、渐变和投影。
- 管理员/员工新增已加载列表的前端状态分组，与原搜索取交集；切分类重置页码和勾选，导出仍遵循当前结果/勾选范围。不冒充全库统计，不改四项顶部统计。客户继续原服务端分组查询及竞态门闩。
- 直接复用共享 classifyStatusGroup：卸柜在途、泰国仓含派送、旧未知状态兜底在途。认证/API/共享状态定义未改；三端原有功能46项、分类与5种宽度53项、实际分类XLSX下载4项、18套非数据库回归、3套类型检查和生产构建通过。浏览器业务请求全部合成拦截，不是实际账户或线上验收。
- 视觉回归发现旧重要样式覆盖选中底色，已修复；测试修正员工显示号优先级、跨页取样、产品嵌套表选择器及变异样式优先级，保留失败记录。原件/修改包/补丁/精确命令记录/保守回滚位于 `.audit/2026-09-05/shipment-categories/`，只同步本地，不推送、部署或改数据。

## 2026-09-05：文字优先视觉方案本地落地

- 用户确认第三版效果图后实施：浅灰导航、墨色文字、少量暗橙，去掉装饰图标、空状态小货车、纸纹、客户分区色条/标签和多余说明。共享 ledger-shell / ledger.css 覆盖三端及子页，登录控件沿用同一色板；删除历史样式长注释，保留会话、SSR、权限与产品行关键约束。
- 保留真实表格全部列/明细/滚动、导出/筛选、六状态桶和本月统计；未照搬效果图里的示例字段或数字。侧栏仅合并同地址重复入口，全部唯一目的地和角色分组保持；三端高亮统一为当前目的地。
- 独立本地 Chromium 合成API：三端主流程46项、文字优先视觉/移动端/子页49项、登录34项、会话37项通过；视觉检查发现旧规则压小字号并修复，保留失败记录；生产构建浏览器另检出两处分区装饰分隔条残留，已删除并复验。真实XLSX下载9项与既有基线单元格矩阵一致；14项源码/AST契约确认认证/API/共享业务定义未改，页面非渲染语句未改。
- 18套非数据库回归、测试/API/Web三套类型检查及25页生产构建记录见 `.audit/2026-09-05/text-first-ui/verification.md`；浏览器测试不代表真实账户/生产验收。仅同步本地源文件，不推送/部署/数据库操作；原件/补丁/验证和防覆盖回滚位于同目录。

## 2026-09-05：双标签误退出与会话竞态修复（本地，未部署）

- 真浏览器复现：A已在`/staff/container-loading`，B打开/刷新登录页，旧挂载逻辑删除同源auth_session_v1，A跳到`/login?from=...`；全部合成业务响应200，无401。上一轮单页测试漏掉了同context保留登录标签的场景。
- 登录页改为已有会话只读；匿名/坏会话仍清残留客户缓存。只有新账号验证成功、明确退出/改密或本会话终局401才清理；明确退出同时删除legacy键，避免旧身份迁回。未增加免密码入口，后端身份校验不变。
- RoleShell跳转前重新读共享会话，旧null事件不再踢掉新登录。页面绑定userId/companyId/role；换身份先隐藏旧业务内容再重载，避免旧客户数据残留；同身份仅token变化保留页面和未提交输入。
- API响应关联请求发出时的Bearer（WeakMap，不落盘、不日志输出）：旧401不清新会话；当前令牌真正过期或连续3次401才清理。已全局核对直接fetch→parse调用者，仅接请求元数据包装，不改接口/请求体/业务字段。
- 双标签真实组件37项从基线16绿21红到37全绿；三端同identity换token保留草稿、跨identity重建、失败登录和主动退出均覆盖。真实Next的8轮双标签刷新及实际FastRefresh也保持会话。新增25项会话函数与26项API竞态测试接入npm/CI；完整最终回归、源码哈希及保守回滚见`.audit/2026-09-05/session-stability/verification.md`。
- 保留上一轮三端UI成果，不推送、部署、迁移、重启原服务或读写业务库；浏览器API全部合成拦截，未操作用户真实浏览器/凭证。

## 2026-09-05：三端统一细节优化与本机登录修复（未部署）

- 延续上一轮员工源码，推广运单工作台至管理员/客户；共享导航、详情、Toast、产品小表统一可访问性。管理员完善选择/导出/刷新反馈；客户保留六状态桶与手动查询，明确建单日期，预报表单适配手机并防重复提交。未改 API、状态、金额、列字段或客户隐私口径。
- 真浏览器复现本地 `0.0.0.0` 启动时，`127.0.0.1` 的 Next 16.3.3 开发连接被拒，页面未 hydrate；仅重启/清缓存均未解决。同源 localhost 正常。按本地框架文档精确增加 `allowedDevOrigins: ["127.0.0.1"]`，127/localhost均正常，未列明来源仍被拒；生产头与API转发不变。已恢复原 launchd 托管前端，后端未重启，旧缓存保留于本轮审计目录。
- 登录补真实表单/浏览器自动填充、密码显示与大小写提示、可读错误和同步提交锁，密码不再被擅自trim；输入与交互已在真实3000验证，账户成功认证未代替用户验证。
- 隔离 Next 三端桌面/手机合成API浏览器46/46；登录4宽度34/34，公共交互21/21（含禁用fieldset焦点红→绿）；16套非数据库脚本、测试/API/Web三套tsc、Next25/25构建全过。所有浏览器业务数据/写请求均为合成拦截，不是线上或真实库验收。
- 本轮基线是上一轮员工精修后的未提交工作区，不是直接回退main。完整输入/输出、截图、补丁、修改包和保守回滚在 `.audit/2026-09-05/global-ui-refinement/`；未推送、部署、迁移或读写业务数据。登录排查仅检查本机API健康/空字段响应及测试库DNS/TCP可达性。

## 2026-09-05：员工运单管理页面细节精修（本地，未部署）

- 新增员工专用 workbench 查询外观：常驻标签、回车定位、清空条件和隐藏条件摘要；保留即时筛选和管理员默认组件行为。工具栏、分页、导出区域分开排列，跨页选择范围明确。
- 固定外层表头，恢复手机端原生表格布局；浏览器发现旧全局 padding 导致产品三行裁切，已在员工页面范围恢复 24px 产品行。桌面列顺序/列宽、1/2 项居中与多项滚动口径不变。
- 增加单号复制成功/失败反馈、键盘焦点、半选状态及导出处理中防连点、逆日期提示；未改接口、金额/状态规则或数据库。
- 隔离 Chromium 合成响应：5 种宽度 60 项、真实 XLSX 导出对照 9 项、共享搜索组件 13 项、详情开关 2 项通过；2 种样式变异均被几何断言抓住。16 套非数据库回归、三套 tsc、Next 25/25 构建通过。
- 基线浏览器 30/41：11 项红包含本轮新增能力缺失及原有产品裁切/手机列错位；不是生产检查。证据、截图、原件哈希、补丁和已验证回滚在 `.audit/2026-09-05/staff-ui-refinement/`。未推送、部署或接触真实 API/数据库，原有 3000/3001 服务未操作。

## 2026-08-29：批量建单的无效唛头提示中文化

- 员工批量建单匹配不到客户唛头时，接口改为返回“唛头不存在或不属于当前公司，请核对客户唛头”，不再把内部字段名 `invalid clientId` 直接显示给员工。
- 运输方式、箱数、尺寸等任何字段的解析错误，以及正式创建阶段的错误，都会同时显示“运单号 + 唛头”；原表缺值时显示 `—`。
- 真实路由夹具确认返回 400 中文提示；100 运单/300 明细解析回归及 API 类型检查通过，匹配规则与数据库均未改变。

## 2026-08-27：员工批量创建支持一票多产品/多尺寸

- Excel 改为“一行一种产品规格”，前端先按运单号分组，再将同号多行作为一个 `products[]` 请求创建；连续明细可省略运单号和公共字段，同组冲突会在写库前提示。
- 总箱数、产品数、重量、体积均以产品明细汇总；预览修正运单号表头错位并显示“原始行数 → 运单数”，下载模板增加填写说明。
- 同一份 100 运单 × 3 明细真实页面回归由修前 100 成功/200 重复失败，变为 100/100 运单成功；数据库 100 订单、100 运单、300 产品，单票 9 箱/88kg/1.928m³ 全部一致。
- 第二轮换成全字段矩阵：2 客户、4 仓库、海陆运、箱袋、100 行公共字段继承；订单/运单/产品每票 49 个字段点、共 4900 项逐值对比，0 mismatch。
- Web 类型检查 0 错、25/25 生产构建通过；测试业务数据已全部清理归零。证据在 `.audit/2026-08-27/batch-create-multi-product-100/`，未部署、未推送、未跑迁移。

## 2026-08-26：尾端导出 P1 / P2 / P3 修复复核通过

- 当前路由处理器 → 当前导出器 → XLSX 定向实跑确认：未知体积/重量保持空白，显式 0 仍输出 0；中文 `E16/F16/G16` 公式保留。
- `loaded_volume_m3` 经 schema、初始迁移和本地库只读查询确认非空；当前 15 行为 0 个 null。nullable 前端类型、`lineTotal()` 合计语义及进度条 CSS 清理均通过。
- 额外发现既有 P2：财务汇总接口把 nullable 订单指标变成 0，活跃财务页会显示 0；本地 184 张订单中 150 张至少一项为空。另有导出 API nullable 文档缺口和一个未挂载轨迹组件的休眠契约风险。
- 未重复 tsc/build/浏览器验证，未改业务源码、XLSX 模板、数据库结构或数据；证据在 `.audit/2026-08-26/p1p2p3-rereview/`。

## 2026-08-26：尾端 UI 三处与导出留空复查

- 全空行收起、进度文字保留、柜价 null/0 按钮判断通过；真实浏览器确认分隔线与 10 票 WD 完整显示。
- 发现阻断项：客户导出 API 仍把 null 体积/重量转为 0，所以 builder 的留空逻辑在真实按钮链路不生效；整柜缺失重量同样转 0，前端接口类型也未改为 nullable。
- 10 票/11 明细的续页、序号、SUM、泰文与 null 格子真跑通过；同时确认正式模板无打印区域/fit-to-width，LibreOffice 会把泰文右侧列拆成额外窄页，该问题是模板原有而非本轮回归。
- 进度条还留有死 CSS 和 5px 无效间距。本次未改源码/模板/数据，完整证据在 `.audit/2026-08-26/ui-export-review/`。

## 2026-08-26：尾端派送界面收敛为单色工作表

- 管理员与员工共享的尾端工作台只调整格式：去掉橙/绿卡片边框、彩色状态胶囊和彩色进度条；页签改为下划线，筛选改浅灰选中，层级改由字号、留白和细分隔线表达。
- WD → 客户 → 地址/运单结构、创建/追加、签收、轨迹、删除、客户导出和柜价映射逻辑均未改变；柜价页同步改成白底表格，并在手机端增加横向滑动提示及固定柜号列。
- 修复手机顶栏菜单按钮背景与三条菜单线同为蓝色、看起来像空方块的问题；390px 页面无全局横向溢出，管理员/员工 1440px 与管理员 390px 均已真实截图检查。
- WD 搜索完整分组、追加上下文、错误态和客户导出参数回归通过；格式计算样式回归通过；Web 类型检查 0 错、25/25 构建通过。审计、截图、补丁与回滚在 `.audit/2026-08-26/lastmile-format-simplification/`，未改 API、数据库、模板或导出器，未部署。

## 2026-08-26：尾端派送映射全链路复核

- 本地数据库、API 与真实浏览器逐项核对：装柜管理 18 柜 = 柜价列表 18 行，柜号/柜型/状态/价格字段 0 mismatch；当前 0 柜填写价格。
- 派送记录 8 行 = 数据库 8 行，组成 6 张 WD、7 个客户；客户/收件人/电话/地址/品名/件数逐字段 0 mismatch。可派送运单 155/155 全部取完。
- 客户导出仅返回所选 `deliveryNo + clientId`，无非空柜值、无价格字段；9 种现有柜状态全部中文显示。
- 修正清除 CNY/USD 柜价后币种短暂保留的问题，清除后立即回显 THB；通用 `ShipmentItem` 补齐收件人姓名/电话类型。
- 临时价格保存、列表读取、数据库关系与清除恢复完整往返通过；Web 类型检查 0 错、25/25 构建通过。记录在 `.audit/2026-08-26/lastmile-mapping-check/`，未留下测试价格。

## 2026-08-25：尾端派送工作台按 WD / 客户 / 地址重构

- 管理员、员工端统一使用同一套 WD 工作台：一张 WD 代表一辆车的一趟派送，卡内按客户分组，再展示各收货地址和运单；司机、车牌、电话、日期只在 WD 层显示一次。
- 搜索改为先构造完整 WD 再判断命中，修复搜索单票后把 `1/3` 误算为 `1/1`；追加运单改为独立的“追加到 WDxxxxxx”上下文。
- 管理员柜价维护移到独立页签；派送单、柜价均区分加载中、接口失败、真实空数据和筛选无结果；柜价行保存不再覆盖其它未保存草稿。
- 本地真实浏览器可见 6 张 WD、8 个客户组和 7 个已填写地址，390/768/1440px 无全局横向溢出；客户导出按钮真实传 `deliveryNo + clientId`。
- Web 类型检查 0 错、25/25 构建通过；API 仍为历史 19 错。补丁、验证、回滚和截图在 `.audit/2026-08-25/lastmile-ui-optimization/`；未改数据库、迁移、模板或导出器，未部署。

## 2026-08-25：本地尾端派送“没数据”已恢复

- 本地 API 日志复现 `container_lastmile_prices` 表不存在；已有柜价迁移仅新增表/索引/外键，已执行并登记 applied。
- Chrome 旧令牌同时已过期；用当前 active admin 的签名令牌刷新独立本地浏览器 profile，未打印或落盘令牌。
- 修后真实 API：尾端派送 8 行、柜价映射 18 柜、装柜管理 18 柜均返回 200；Chrome DOM 可见 6 个 WD、4 个柜号，无空态或错误提示。
- 完整记录在 `.audit/2026-08-25/local-system-no-data/verification.md`；没有改业务源码、模板或历史业务数据。

## 2026-08-25：两个新导出功能完成本地全链路复测

- 真实 localhost HTTP 跑通按柜导出，以及创建 `WD000501` 后按单一客户导出；HTTP 数据继续经当前导出器和正式模板生成 XLSX，未连接数据库、未部署。
- 修复泰文短页/续页空白行残留假序号、客户备注柜号泄漏、创建 WD 未校验运单公司归属三处问题；跨公司混合请求现在整车原子回滚。
- 整柜 1/25/26/50/51 行和客户 1/10/11/20/21 行分页、多地址中泰配对、重量体积、隐私、公式、模板结构、Web 0 错类型检查及 25/25 构建均实跑通过；API 仍是历史 19 错。
- 两份示范文件在 `outputs/two-export-local-retest-20260825/`；统一验证、补丁和回滚在 `.audit/2026-08-25/two-export-local-retest/`。

## 2026-08-25：三端运单列表显示整票体积与重量

- 管理员、员工、客户正式运单列表统一改为“订单合计 → 父子运单家族合计 → 父单自身字段”，全部缺失显示 `—`；旧 `volumeM3/weightKg` 字段保留。
- `/staff/shipments?all=1` 只给父单下发 `totalVolumeM3/totalWeightKg`，子单继续显示自身指标；实际客户列表消费的 `/client/orders` 也已同步，客户备注复用既有柜号净化。
- 真实 localhost HTTP + JWT/session guard、隔离 HTTP fixture、定向路由/前端回归、Web 0 错类型检查和 25/25 生产构建均实跑通过；API 仍为历史基线 19 错。
- 本地复测发现客户正式运单旧调用只取第 1 页 500 条；已改为和管理员/员工端一样按 `total` 翻页，带 `statusGroup` 时按后端页进度停止，红/绿分页回归通过。
- 生产库在只读事务内用 `SELECT` 推演并 `ROLLBACK`：900 张老口径与 30 张已归零父单全部改取订单合计，显示 0 的体积/重量均为 0 张；另有 8 张全链路缺值显示 `—`。
- 本轮未改分柜算法、`split-metrics.ts`、Schema/迁移、历史数据或客户预报单表，未部署；尾端派送导出按钮与 Excel 模板仍是上轮待点验项。

## 2026-08-25：分柜异常提示与 outputs 忽略边界已补齐

- `POST /staff/shipments/split` 只将事务内的 `RangeError` 转成 400 和人话提示；其他异常仍原样抛出。
- 仓库根 `.gitignore` 新增 `/outputs/`；24 个演示文件保留在本地，两个正式 XLSX 模板仍未被忽略。
- `/admin/orders` 的父单体积取数和历史数据均未改，继续等待老板确认“整票总量 / 父单剩余量”口径。
- 只跑了分柜异常两分支和 Git 忽略边界的定向验证，补丁与回滚均实际校验；本轮未登录浏览器点击导出、未连接数据库、未部署。

## 2026-08-25：整柜与 WD 客户派送单导出缺陷已修复

- 修复单箱重量未乘箱数、旧分柜父子重量/体积重复、未知运输方式误写目的地、搜索后导出旧客户，以及多地址信息丢失。
- 客户版按“所选 WD + 单一客户”导出；同一客户的不同收货站点分别生成一对中/泰文工作表，客户文件不含柜号或其他客户。
- 超出模板容量时克隆完整工作表：客户每站点每 10 行一对中泰页、整柜每 25 行一页；跨页序号连续，整柜总票/总件数一致，页内合计仍按本页明细计算。
- 原模板的列宽、行高、样式、合并、固定文案、页边距及打印设置未变；同时同步工作簿关系、内容类型和扩展属性元数据。
- 分柜指标改为按父子家族在最小计量单位上守恒，覆盖历史空值、重复分柜、父单被改回整票、订单总量/件数后改、极小值及三等分舍入边界。
- 前端 25 路由生产构建、API/导出/UI 回归、两份多页 XLSX 的结构/隐私/公式/逐页渲染及回滚均通过；API 原有 19 个类型错误未增加。审计在 `.audit/2026-08-25/container-and-wd-export-bugfix/`，未部署。

## 2026-08-25：整柜与 WD 客户派送单导出已按两个入口落地

- “整柜导出”放在员工端装柜管理：选中柜子后，按柜内实际装货记录生成给尾端拆柜仓的清单，不依赖 WD、司机或车辆。
- “客户派送单”放在尾端派送：建好 WD 后，管理员/员工端均按一个 WD 一张卡展示，可从该 WD 的多个客户中选一个导出签收单。
- 客户导出接口不查询、不下发柜号；中泰文客户页仅含所选客户。两类导出均直接填写已确认的 XLSX 模板。
- 模拟文件已作内容、公式、渲染及 OOXML 结构校验，列宽、行高、样式、合并、页边距和打印设置保持；前端 25 路由生产构建通过，API 原有 19 个类型错误未增加。
- 实现、补丁、验证与可执行回滚在 `.audit/2026-08-25/container-and-wd-exports/`；未部署生产。

## 2026-08-25：尾端派送价格按装柜管理的柜子映射

- 管理员端「尾端派送」新增“柜子派送价格”，柜子直接读取装柜管理的 `Container`，不再另建柜号，也不关联 WD、司机或车辆。
- 每个柜子最多保存一条内部价格，包含金额、币种和备注；价格留空保存可清除映射。
- 新增公司隔离且仅管理员可读写的柜价接口，以及 `container_lastmile_prices` 数据表迁移；员工端和客户端均不下发该价格。
- 前端生产构建通过，API 路由覆盖读取、保存、清除、负数校验、跨公司柜拒绝和员工拒绝；API 原有 19 个类型错误未增加。
- 记录、补丁和回滚位于 `.audit/2026-08-25/container-lastmile-prices/`；数据库迁移尚未部署到生产。

## 2026-08-25：客户签收模板删除柜号

- 只修改客户签收模板，拆柜仓模板继续保留柜号。
- 中文页顶部改为唛头、电话、收货地址；泰文页同步删除柜号、封条号、柜型、目的港等柜相关区，改为同样三块客户信息。
- 两页残留示例值 `13` 已清空，固定声明、明细表、签收区和公式保持；完整渲染及公式扫描通过。
- 记录、补丁和回滚位于 `.audit/2026-08-25/customer-template-no-container/`。

## 2026-08-25：WD 派送导出方案已按用户要求回退

- 用户确认业务主维度应为“柜”而不是“车/WD”，决定暂缓设计。
- 已回退本次新增的导出 API、管理员 WD 卡片分组、管理员/员工导出入口、XLSX 导出器和 JSZip 依赖。
- 两份用户确认的正式模板继续原样保存在项目中，但暂不接入任何导出逻辑；尚未实现柜号维度的新方案，等待用户重新确认。

## 2026-08-25：尾端派送正式模板已接入

- 用户确认 `/Downloads/模板(1).xlsx` 为整车/拆柜仓模板，`/Downloads/模板.xlsx` 为单客户中泰文签收模板；项目模板资产已逐字节替换为这两份文件。
- 整车正式模板数据区为 25 行（10–34），合计行 35；首列按模板改为序列号，同一运单多产品合并序号/运单号/备注。
- WD009998 多客户、多地址模拟导出通过；三张工作表公式错误为 0，模板占位值已清空，客户版未带柜号或其他客户。
- OOXML 校验确认列宽、行高、样式、页边距、打印设置和固定文案保持；记录和回滚位于 `.audit/2026-08-25/lastmile-export/new-template-switch/`。

## 2026-08-25：尾端派送按 WD 分组与双粒度导出

- 管理员端尾端派送由一票一行改为一个 WD 一张卡，一趟车可包含多客户、多地址、多运单。
- 管理员/员工两端都可导出整车 WD，或从 WD 内选一个客户导出客户签收单；客户版不输出内部柜号。
- 新增稳定导出数据端点；导出器已改为直接使用用户原 XLSX，列宽、行高、页边距、打印设置、样式和声明文案全部锁定。
- 前端生产构建、实际 HTTP 端点、多客户 Excel 范围/柜号隐藏、回滚脚本均已验证；记录在 `.audit/2026-08-25/lastmile-export/`。

## 2026-08-22：运营看板映射问题修复

- 修复 KPI/柜量/利润/预警不随 10 秒自动刷新、旧 `DELIVERING` 柜误报卡住、利润总计只算最近 20 条、供应商报价跨运输方式/季节/币种误比较。
- 状态图标题由“订单状态分布”改为“运单状态分布”，报价提醒显示运输方式、季节和币种。
- 前端 25 路由构建通过；浏览器确认 overview/ops 均在一个刷新周期内请求 2 次；API 与图表 9 项对账通过。
- 修复包、验证和回滚：`.audit/2026-08-22/dashboard-fix/`。API 原有 19 个类型错误未增加。

## 2026-08-22：系统全面排查完成

- 报告：`docs/系统全面排查报告-2026-08-22.md`；完整复现日志：`.audit/2026-08-22/`。
- 本地前端生产构建、Prisma 校验、25 个登录态只读接口和 13 项测试库数据一致性检查通过。
- 发现 4 个上线阻断风险：令牌在封号/改密后仍有效、员工仓库写权限固定放行、尾程派送写接口缺公司隔离、代码先于数据库迁移上线。
- API 类型检查有 19 个错误且 CI 未覆盖；测试库 17 个迁移中 10 个未登记执行；依赖审计有 high 项；项目没有自动化测试。
- 本次只读检查测试库，没有部署、推送、调用写接口或覆盖排查前已有工作区改动。
