## 2026-09-20：轮询去重不能吞掉写后的失效刷新

- `if (inFlight) return` 可以去重普通轮询，但不能用于写操作完成后的刷新；旧 GET 的快照可能产生于写前。显式写后刷新应合并排队、让等待者等待补发结果，旧请求失败/超时也不能取消这次刷新。
- 测试必须执行真实写回调，按「旧 GET 未返回 → 写入成功 → 旧 GET 返回 → 新 GET 返回」控制时序；先 resolve 旧请求再调用刷新只测到去重，测不到丢刷新。还要覆盖连续写入、过期响应不落地、最终失败与微任务结束边界。
- HTML 默认折叠连续空格，条码原值保留不代表纸面文字一致。运单号用专用保留空格样式，浏览器核 `innerText`/字符宽度，而不是仅看源码字符串或无障碍树（它也可能折叠空格）。不顺带改变入库编号规则。

## 2026-09-12：按实际页面资源验证生产构建，不猜编译器目录

- 本地Next webpack副本有static/chunks/app/<route>/page-*.js；生产默认Turbopack是扁平哈希chunk。用本地布局查线上会得到find不存在，这是探针假设错误，不代表应用失败。
- 从真实公网HTML提取script src，校验同源静态路径，下载内容后逐个对正在运行镜像的文件SHA；兼容两种构建结构，也能验“镜像里有、实际网络拿到的也是”而非仅grep源码。归因/修正探针期间不擅自重建服务或回退正常业务。
- 区分资源含文本、真实组件函数执行和真实账号交互三层；前两项通过不冒充浏览器验收。

## 2026-09-12：正常启动任务也会使严格数据摘要变化

- 应用镜像切换前须核启动任务的写入副作用；原有汇率同步会在启动时把过期缓存更新为供应商行情。隔离internal网络里外部获取失败，未必能复现线上该正常副作用，不能从恢复库全等推出线上启动绝不写库。
- 严格摘要检出后先真实回退应用、保存第一轮证据，再区分业务正常写入与异常破坏。不为了“全绿”回写旧数据或随意排除表。本次独立核唯一汇率行与供应商一致后，以当前状态重拍基线，原全部45表严格比较第二次通过。
- 报告区分“没有操作员SQL/结构迁移/数据覆盖”“运单核心指标完整”“系统自动缓存曾刷新”和“第二次切换全表全等”。当前RestartCount=0也不代表过程中没有主动重建应用。

## 2026-09-12：修复也要受独立反例检验

- 派送自动日志按稳定系统ID来源保护，不按可自由填写的备注前缀识别；否则普通“尾端派送前检查包装”也会误拦。保护需在锁内服务端执行，前端canDelete只同步能力提示。
- 长品名不能只验汉字/换行。宽拉丁W/M、泰文组合声调、空段和真实字号应独立测试；PDF能抽出ENDW但纸面仍裁切，必须查看栅格图。物理分页可能随行高增加，别把模板分页定义不变说成打印页数永远不变。
- 复核者审自己未实现的分区，用独立严格桩/真实消费者/真实输出提出新反例；修后让原复核者同输入再验，不以实现者全绿自述代替复核。

## 2026-09-12：真实动作、真实落库和真实打印才覆盖功能边界

- 背景：4d2a7ba全套23自测/三套tsc/构建全绿，扩展本地测试仍发现撤销与删轨迹联动、漏校验的管理员写入口、客户端整票体积少乘箱数、长品名打印裁切。测试要跨完整业务链，别把单路由/模板字段测试当成全需求覆盖。
- 枚举别名表用普通对象索引时会命中继承的constructor；必须限定自有属性/Map，且测试经过JSON序列化后的实际请求值。
- XLSX的全部字符存在、PDF文字层能抽到，并不证明打印能看全；按模板真实字号＋显式换行验行高，用真实渲染图及控制组验证，标清字体回退和未测平台。
- 本地Prisma5.22真测：rolled_back_at有值的失败记录可重试，未解决失败记录才P3009；别只看finished_at为空就指导改生产迁移历史。
- 本地并发夹具需要独立连接及真实锁等待；PG事务内重复读统计视图要清统计快照。隔离PG小连接上限时，审计API/探针应显式限池，防测试环境本身挤满连接造成假失败。

## 2026-09-07：列数对了，不等于表头与数值视觉对齐

- th/td受内联和旧主题不同padding覆盖时，同列边界一致，文字仍会差4px；总箱数只给td右对齐而表头靠左，偏差更明显。数字列应成对标记表头/单元格并统一边距。
- 验收除了逐列值不变，还要用真实浏览器getComputedStyle与Range测文字右缘；覆盖三端、窄屏和滚动/勾选。只数th/td或看源码有right会漏掉这种问题。
- 几何回归要真实撤掉边距或表头标记看测试变红。页内局部值需从原AST一同装入渲染夹具，漏绑定属于夹具问题，不能算业务回归。

# 项目踩坑记录

## 2026-08-27：批量导入必须先按业务实体分组，不能把 Excel 行当成订单

- Excel 的一行可能只是同一运单的一种产品或一种尺寸。若前端逐行调用“创建运单”，同号第二行起只会触发唯一约束，最终留下每票第一条明细和错误的箱数、重量、体积。
- 正确顺序是“逐行解析与定位错误 → 按运单号分组 → 校验公共字段一致 → 汇总并一次提交 `products[]`”；后端仍应以产品明细为事实源重算整票合计，避免信任调用方传来的重复合计字段。
- 回归要同时断言三层：页面显示的原始行数/合并运单数、接口成功数、数据库订单/运单/产品数量与件重体守恒。只验证 Excel 被读出 300 行，并不代表300行被正确归入100票。

## 2026-08-26：运营工作台的层级优先用排版，不要用多套状态色堆叠

- WD、客户、地址本身已有三级业务层级；再叠加橙/绿左边框、彩色胶囊、彩色进度和实心页签会让同一屏出现过多视觉主角。运营密集界面应优先用字号、字重、留白和 1px 分隔线，颜色只保留给真正的主操作与危险操作。
- 全局 `button:not(...)` 背景规则会覆盖局部 `background: none`。手机菜单按钮因此变成蓝底，而三条菜单线也是蓝色，最终看成无含义的纯色方块。图标按钮要用更明确的透明背景覆盖，并在 390px 真机尺寸检查按钮与图标的计算颜色，而不能只看 JSX 结构。

## 2026-08-26：删除可选映射后，前端派生字段也要恢复服务端默认值

- 可选柜价记录删除后，服务端 GET 会回到默认币种 THB；若前端只把价格清空、仍保留原草稿的 CNY/USD，页面会短暂显示一个数据库里不存在的状态，直到刷新才纠正。删除成功时要同时归一所有派生默认字段。
- API 已下发的收件人姓名、电话、地址要在通用响应类型中完整声明；局部代码使用 `any[]` 即使运行正常，也会让未来字段改名时失去编译期保护。

## 2026-08-25：分组列表必须先算完整实体，再搜索；失败态不能伪装成空数据

- WD 是业务实体，内部有多票运单。若先按搜索词过滤运单行、再按 WD 分组，搜索到一张已签收运单会把真实 `1/3` 误算成 `1/1` 并显示“派送完成”。应先对完整 WD 计算客户、地址、签收进度，再用 WD 内任一字段决定整张卡是否命中。
- 列表需要明确区分“加载中 / 请求失败 / 数据确实为空 / 筛选后无结果”。请求失败时直接落入空数组会让用户以为生产没有数据；有旧数据时可显示刷新错误并保留缓存，无旧数据时只显示错误和重试。
- WD 级信息（司机、车辆、日期）只显示一次；客户级导出按钮必须从当前客户组直接传 `deliveryNo + clientId`，避免下拉残留选择把文件导给错客户。

## 2026-08-25：固定 XLSX 模板分页与分柜指标必须整体守恒

- **背景**：整柜清单和客户签收单直接填充用户确认的固定模板，运单还存在历史父子分柜数据。
- **现象**：拉伸行、动态合并或把多地址塞进固定行高备注会破坏版式；只克隆 worksheet 会漏工作簿元数据；逐票四舍五入或只用“子单是否为空”识别旧数据会让父子总重、总体积变多或变少。
- **教训**：超容量时克隆完整工作表并同步 workbook relationships、content types、extended properties；每页保持原列宽/行高/样式/合并/打印设置，跨页序号连续，“全局总计”和“页内小计”分开处理。多地址用独立站点页，不挤压模板行高。
- **教训**：分柜重量/体积必须以整个父子家族为单位，在 kg/m³ 的最小精度单位上累计分配并校验订单总量守恒；历史空值、父单回写整票、订单件数为 0、总量调低和循环小数都要有回归。
- **教训**：React 导出按钮使用的客户选择属于派生状态，搜索或刷新后必须重新对当前 WD 卡片的有效客户集合做校验，不能继续使用隐藏行留下的旧值。
# 2026-08-25：新增可选关联表未迁移，也会让整个列表查询 500

- Prisma `include/select` 了一个新关系时，即使关系业务上“可选”、表里计划先是 0 行，物理表不存在仍会让父表 `findMany()` 整体失败。
- 本地/部署验收不能只看 API 健康检查；要实际请求新增关系所在的页面接口，并检查迁移是否已执行。
- 历史迁移登记不完整时，不要用 `db push`，也不要直接跑全部 pending；先审核目标迁移 SQL，再只执行明确的纯新增迁移并登记 applied。

# 2026-08-25：客户导出要同时守住正文隐私与写接口公司边界

- 客户 XLSX 即使没有 `containerNo` 字段，`shipment.remark` 仍可能包含“装入柜子 XXX”；所有客户导出数据都要复用统一的正文净化函数，不能只靠模板不显示字段。
- 创建 WD 的 `shipmentId` 是外部输入；派送记录虽然写入当前 `companyId`，若不先按 `{ id, companyId }` 验证运单，仍会跨公司改原运单状态和轨迹。
- 多票创建必须在同一事务内先锁行、逐票验证归属再写；中途遇到跨公司运单时整车回滚，并用副作用计数回归证明没有部分提交。

## 2026-09-20：写成功不能被读失败改写；界面快照失效要覆盖请求发出到返回

- 账号写请求成功后再刷新，若共用一个 catch，会把读失败显示为“添加/封禁失败”，诱导重复操作。写 catch 只包写请求，刷新独立报告；服务端已确认的状态可直接更新，但必须防旧列表覆盖。
- 仅在写响应回来时设置刷新 pending，挡不住“库已提交、写响应未到、旧GET先返回”的窗口。写开始和结束都改变读版本，写期间的快照也不落地；版本检查与写后补发是两个职责。
- allSettled 会等每个读取落定，必须同时考虑无限挂起；本轮对旧列表接口只设等待截止，不虚称取消底层请求。不能承诺固定轮询间隔内刷新成功。
- 共享 Promise 足以作为单飞状态，避免另一个 boolean 与 Promise 不一致；缺状态时返回 resolved Promise 会假报完成，不能当作健壮性修复。

## 2026-09-20：dsh闭环的写截止、守卫和验证汇总教训

- 给读取加“写在途时不落地”门槛，也必须保证写有有界结束。否则一个挂起POST会让成功的轮询数据一直被静默丢弃；必须finally释放，并处理不响应AbortSignal的等待边界。非幂等写超时只表示结果未确认，不能自动重试或宣称服务端回滚。
- 总览/列表/创建客户修了读写版本边界，要检查相邻的编辑客户路径；同一个列表不能只保护部分会改变展示字段的写入口。
- **全量通过不能看尾巴**：本轮50命令中间mark-display为1，尾部类型/构建为0，我漏核却报全绿。以全部逐项exitCode为准，任何非零汇总成失败退出；保存失败记录再修复，不能覆盖后假装一直绿。
- 实际Error.name被客户名守卫误报时，保留原有严格守卫，使用符合已有异常命名规范的实际Error对象；不要给客户对象换成error以躲规则。正向通过还需反例确认真实客户名泄漏仍被拦。
- 送审副本不仅同步代码，manifest列出的文档也必须逐字同步；构建副本的旧docs不等于实际交付docs。复核完成后的收尾文字另入最终manifest，不冒称复核员已看过。
