# 复核第 2 轮：显示客户的地方一律显示唛头（补漏）

## 审什么

分支 `fix/mark-display`，**审两个提交合起来**：`git diff b446e29..HEAD`（第 1 轮是 `cf65ee5`，这一轮是它后面那个提交）。基线 `b446e29` 是线上正在跑的版本。没推没上线。
第 1 轮的说明在 `docs/复核-唛头显示-2026-09-19.md`，背景看那份。

## 老板这一轮新定的（原话）

- 显示唛头时旁边要不要带名字？——「显示唛头就行了」：**只显示唛头，不带名字**。
- 客户下预报单时自己填的那个「唛头」（`mark` 字段，线上多数跟账号不一样，如「XPP-0015 XHH5588」「111」）——
  「都标着『唛头』了，为什么还要改？？？你只要知道账号=唛头就行了啊」：**原样不动，不换成账号**。
  这一条管到所有显示 `mark` 的地方（财务页仓库版那一行、各处「唛头：…」、导出表的「唛头」列、编辑框、客户端、代理端）。

## 第 1 轮两位报的，这一轮怎么处理的

| 报的问题 | 处理 |
|---|---|
| 派送签收单泰文页 E31「收货人」：没填泰国收货人、地址簿也没联系人时退到客户名字（线上 1651 张全没填，265 张会印名字） | 后端三条兜底链去掉 `client.name`：`admin-ops/routes.ts` 签收单导出（`contactName`）、同文件派送列表（`receiverName`，工作台上改显示「收货人未填写」）、`loading-manifests/routes.ts` 装柜清单导出。这两个导出接口连客户名字都不再查、不再发 `clientName` |
| 财务「客户 / 唛头」列普通版显示名字，按唛头搜不到 | `finance/routes.ts` 改发 `clientId`。**仓库版那一行显示的是客户自填的 `mark`，按老板上面那句原样不动** |
| 柜子收款客户明细显示名字（还拿名字当 React key，同名会撞） | `admin-ops/routes.ts` 字段 `name` → `clientId`，前端类型和页面同步 |
| 员工批量下单预览「客户ID」列显示名字 | 显示 `row.clientId` |
| 运费配置客户价格列表：名字粗体 + 唛头灰字 | 只显示唛头 |
| 「唛头 · 名字」这种（尾端派送工作台客户标题、员工 / 超管创建订单和整柜询价的选客户下拉、代理调价报错提示） | 只留唛头。下拉选项值就是唛头，选中后按唛头认；**按名字搜不出来了**（下拉里不显示名字，浏览器也不会按名字列出来） |
| 员工仓库版导出表「客户名」列（第 1 轮我保留了，两位都说不对） | 表头改「客户」，内容和「XX 小计」行改唛头；旁边客户自填的「唛头」列不动 |
| 客户自己的仓库版页面顶上显示内部录的名字 | 接口不再发 `customerName`，改发 `clientId`，页面显示唛头 |
| `/client/prealerts` 给客户发了 `clientName`（客户页面没用） | 不发了 |
| 测试太死，换写法就拦不住 | `test-mark-display` 重写，见下面 |
| 死字段 | 删了：长期价下限里的 `clientName`、导出类型里的 `clientName`、建柜时查的名字 |

顺手补的同一类：尾端地址簿（唛头 + 名字 → 只唛头，按名字照样能搜）、超管客户管理「封禁」确认框（报名字 → 报唛头，同名有好几个账号）、运单筛选格的标签「客户名」→「唛头 / 客户名」（本来就两样都能搜）。

## 没动的（请确认判断）

- 老板点名保留名字的：员工运单「运单所属用户」、员工运单导出「归属用户」
- 代理端（`components/agent`、`app/agent`、`[agentSlug]`、`agent-portal`）
- 「客户管理」页里的「客户名字」
- 客户自填的 `mark`（见上）
- 登录接口返回自己的名字（`auth/routes.ts`），老板知道，这次不动
- 签收单上的收货人**电话**没填时仍退到客户电话（这次只管名字）
- 内部员工 / 超管接口还发着 `clientName`，给「按名字也能搜」和保留的两处用

## 测试

- `scripts/test-mark-display.ts` 重写（10 项）：把页面（除代理端）和接口（除代理端、登录）里**每一处**读客户名字的地方都找出来（`clientName` / `customerName` / 客户对象上的 `.name` / `xxxClient…find(...).name`），逐行对允许清单；接口再按文件限处数，往外发的 5 个文件一处都不许有。另有正面断言钉住每处显示的是唛头、客户自填 `mark` 没被换、保留的两处没被动；第 10 项自检扫描能抓到 `||`、多空格、先赋值再显示、`${id} - ${name}` 这些写法。
- `test-lastmile-export` 加第 19 项：收货人和联系人都空时 E31 留空、不印 undefined；填了收货人照印。第 18 项夹具故意塞了名字，证明生成器不会印。
- 变异验证（临时副本，17 种）：详情 `||` 写法、批量预览先赋值再显示、两条收货人兜底加回名字、财务 / 柜子收款改回名字、运费配置、两种下拉拼名字、工作台标题、代理提示、客户仓库版接口、`/client/prealerts`、导出小计、地址簿、客户自填唛头被换成账号、「运单所属用户」被改——**17 种全红**，还原后全绿。
- 全套 + 三份 tsc：见提交说明。
- 浏览器实测（本机副本 + 测试库 `zz_mark_`，两个账号同名「测试杨先」，测完删净）：接口层签收单导出 `contactName` 为空、派送列表收货人为空、财务 / 柜子收款显示唛头、客户两个接口不带名字；页面上尾端派送卡片（标题 ZZMARK01、「收货人未填写」）、柜子收款两种柜的客户明细、财务列表 + 按唛头搜、运费配置、尾端地址、封禁确认框文字、员工创建订单下拉（只列唛头、打唛头后「已选唛头」自动填上）、客户仓库版页面顶上。全程控制台无报错。员工仓库版导出 Excel 没在浏览器点（会下载文件），靠源码测试。

## 请重点看

1. **还有没有漏的**，尤其往外发的（给客户、收货人、第三方的纸 / 文件 / 接口）。按行为找，别只按字段名。
2. **测试的允许清单**有没有放得太宽（某一行既是搜索又顺便显示了名字、类型声明那两条通用规则会不会放过真显示）。
3. **客户自填 `mark` 原样不动**这条有没有被我哪处误改。
4. 去掉名字兜底后，**有没有地方变成空白或 undefined**（签收单、工作台、导出、客户页面）。
5. 创建订单选客户**只能按唛头搜**，会不会让哪个流程走不通。

只报问题：严重程度（泄漏名字到外部 / 显示错或空白 / 功能不对 / 小问题）+ 文件:行 + 触发条件 + 实测还是读代码推断。不要改文件。
