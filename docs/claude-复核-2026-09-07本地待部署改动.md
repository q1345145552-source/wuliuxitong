# Claude 独立复核：2026-09-07 本地待部署改动

> 本文件是给 Claude 的审查任务，不是审查结论。当前终端 Claude Code 2.1.158 的 `auth status` 返回 `loggedIn=false`，尚未执行 Claude 独立审查。不要把编写此文件当成审查通过。

## 提示词正文

你是湘泰物流系统的独立审查员。请检查本地待部署改动有没有引入 bug，以及代码是否具备部署条件。只做审查，不修改业务代码，不提交、不推送、不部署。

### 1. 固定审查范围

仓库：`/Users/liuyujiang/湘泰物流系统/MyWebSite`
分支：`main`
固定基线：`88918ac44eb49e8841e18246c6774944820bef88`（88918ac）
范围：该基线到当前工作区的未提交改动，含下面三个未跟踪新文件。不是只审最后一次功能栏颜色，也不是用`main...HEAD`得到空diff。

- `/Users/liuyujiang/湘泰物流系统/MyWebSite/.github/workflows/ci.yml`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/apps/web/src/app/admin/page.tsx`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/apps/web/src/app/client/page.tsx`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/apps/web/src/app/ledger.css`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/apps/web/src/app/staff/page.tsx`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/apps/web/src/modules/shipment/ShipmentTableGrid.tsx`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/docs/journal.md`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/package.json`
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/apps/web/src/components/admin/AdminShipmentDetail.tsx`（新文件，未跟踪）
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/scripts/test-admin-shipment-detail.ts`（新文件，未跟踪）
- `/Users/liuyujiang/湘泰物流系统/MyWebSite/scripts/test-shipment-list-display.ts`（新文件，未跟踪）

冻结的完整diff（包括新文件）：`/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-07/claude-review-request/review-scope.patch`
冻结的文件清单/hash：`/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-07/claude-review-request/state.json`

先核对业务源码hash。若之后又有修改，明确标出差异和实际审查边界，不静默混入。`docs/journal.md`在冻结后会追加“已准备复核材料”的文档记录，该新增记录与本提示词本身不是业务改动；可排除。

### 2. 老板确认的需求（判断是否符合需求的依据）

1. 三端运单主列表直接显示当前物流状态，放在运单号后，不用最右侧物流轨迹替代。管理员/员工外层单箱数量列让位；详情、编辑、导出和原始产品数量保留。其余现有列及字段值不丢失。
2. 管理员详情原来只有摘要和图片上传框，需补齐只读运单信息、产品明细、状态、数量、体积/重量、地址和备注；无产品行的老单也需合理显示。体积3位、重量2位，空值与0的语义保持，不改底层数据或计费。
3. 运单详情中的柜号文字、值、占位整项去掉；管理员/员工残留一并处理。创建/编辑、装柜管理、数据库关联、导出不在此次删字段范围内；客户详情本来就不显示柜号。
4. 功能栏与主页颜色区分：共用侧栏低饱和深蓝灰，主页暖白不变。仅配色，不改菜单、尺寸、折叠、移动端抽屉、登录会话或业务行为；不加渐变和花哨装饰。
5. 保留既有业务红线：普通运单不新增金额展示/录入；客户不见柜号及不该看的价格；海陆流程分开；正在卸柜仍在途；“异常”是独立关注过滤维度，不改状态机和阶段分组。

### 3. 审查方式与边界

先读`/Users/liuyujiang/湘泰物流系统/MyWebSite/AGENTS.md`和`/Users/liuyujiang/湘泰物流系统/MyWebSite/CLAUDE.md`，再独立检查代码，不直接采信修改者的绿色记录。规范中旧`prisma db push`描述已被后续migration-only口径替代，本任务没有任何执行数据库命令的需要。

如有子代理能力，分开并行审查两个方面：
- **Standards**：明确违反了哪条项目规范，附规则出处。区分硬性问题与风格建议。
- **Spec**：漏做、做错、超出需求的行为，逐项对应上述确认需求。

重点追查：
- 新状态列、colgroup、th/td、colSpan、产品投影索引、固定列和弹性列是否逐列对齐；一/两/三/多产品以及手机横向滚动是否受影响。
- 管理员真实详情入口是否挂载新组件，而不是只测一个未使用的组件。多产品和旧单、字段缺失/0、长文本、图片有无、产品单箱数量等能否正确显示。
- 新详情里的“总件数/总体积/总重量/产品数量/日期”是否忠实于API字段语义；结合真实服务和共享合计函数核对，特别是已分柜父子单、无产品行老单、剩余量与整票量，不能只因类型检查通过就判定口径正确。
- 柜号是否整项移除且没有误删装柜功能、编辑输入、API字段或导出。图片上传/删除处理函数和查询/提交/effect是否保持。
- 侧栏CSS变量的作用域、旧主题覆盖、hover/active/focus、按钮、手机媒体规则、对比度；主页及其他模块是否被染色。
- 两个新测试是否真走源码渲染，并能抓到移除页面挂载、产品投影错位、丢产品、总量取值错等变异；CI是否真的执行、是否需要新增依赖。

只允许本地无数据库验证。不要访问生产、读取环境密钥/数据库备份、登录真实账号或写业务数据。不要操作3000/3001。不要在原仓库跑build（会影响老板正在看的`.next`）；需要构建/变异/生成物时使用独立副本，不复制`.env*`、`.next`、`.git`或审计备份目录。网络如确有需要只用直连，不启用代理或隧道。

### 4. 现有证据只供核对，不等于你的独立验收

- 状态列表：`/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-07/inline-shipment-status/verification.md`
- 详情修复：`/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-07/shipment-detail-repair/verification.md`
- 配色：`/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-07/sidebar-colour/verification.md`
- 真实源码回归：`/Users/liuyujiang/湘泰物流系统/MyWebSite/scripts/test-shipment-list-display.ts`、`/Users/liuyujiang/湘泰物流系统/MyWebSite/scripts/test-admin-shipment-detail.ts`
- 浏览器脚本和合成接口：上述审计目录的`tests/`；运行前读脚本，确认目标是隔离localhost端口、业务API全拦截。它们不是生产真实数据验收。

修改者记录：详情轮跑过21套无库自测、3套类型、25页独立构建和三端浏览器。之后配色轮只做CSS解析/浏览器对照，没有重新构建完整最终版本。你需明确哪些是亲自复跑，哪些只是核对历史记录；建议最终合并快照在副本补跑构建。

副本内可执行的检查入口（不要在原仓库build）：
```bash
npm run test:shipment-list-display
npm run test:admin-shipment-detail
npm run test:frontend-guards
npm run test:session-stability
npm run test:session-api
npm run test:status-group
npm run test:attention-filter
npm run test:product-rows
npx --no-install tsc --noEmit --incremental false --project tsconfig.scripts.json
npx --no-install tsc --noEmit --incremental false --project apps/api/tsconfig.json
npx --no-install tsc --noEmit --incremental false --project apps/web/tsconfig.json
npm run build --prefix apps/web -- --webpack
```
数据库连接和API转发目标置为不可用的本地测试地址，确保测试不误连真实服务；不打印任何已有密钥。发现疑点尽量用最小合成输入或副本变异复现，给出明确输入/预期/实际，不将猜测当确定bug。

### 5. 输出格式

1. **Standards**：问题及规范出处；没有则写“没有”。
2. **Spec**：需求满足情况；问题按P1/P2/P3列出，包含绝对`file:line`、触发条件/操作序列、预期与实际、影响。区分本次引入/既有问题、已复现/只读推断。
3. **实跑记录**：精确命令、结果/退出码；另外列出未验证的范围。不要只说“全绿”。
4. **部署判断**：给出“可以进入部署准备”或“先修再部署”及理由；证据不足则明确写“待补验证”，不能硬给绿灯。说明是否含API变化、迁移或依赖变更，以及部署后应独立验证哪些页面。

当前观察是API/迁移/依赖锁文件未改、package.json只加测试脚本；请自行复核。评估部署条件不等于执行部署授权。即使通过，也不要帮老板推送/上线。
