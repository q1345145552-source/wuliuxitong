# 复核：运单条码 + 运营看板修补（2026-09-20）

> 你是**独立对抗审查员**。只报问题、不改任何文件、不写数据库、不连生产库、不部署。
> 目标：帮老板确认这几批改动**有没有引入新的 bug**。看不到问题就说看不到，不要为了凑数编问题。

## 0. 硬性要求

1. **每条结论必须给证据**：`文件:行号`，或可复现的命令 + 实际输出。不接受「可能」「建议检查」这类话。
2. **区分「代码里有」和「真会发生」**：读代码推断出来的要标明是**推断**，真跑过的标明是**实测**（贴命令和输出）。
3. **报格式**：严重程度（P1 会坏线上 / P2 会用错或看起来坏 / P3 小毛病）+ `文件:行` + 触发条件（什么情况下发生） + 实测还是推断。
4. **最后一句必须明确回答**：这批改动**能不能上线**。
5. **只读**：不要改文件、不要 `git commit`、不要跑部署脚本、不要连生产库（`76.13.181.104` 上的 `xiangtai-postgres` 一律别碰）。
6. **别跑全套测试**（`zsh .audit/tools/run-all-tests.sh` 会连 Neon 测试库、和别人抢），要验证就跑**单个**脚本：`npm run -s test:xxx`；也不要起开发服务器。

## 1. 系统背景（够用就行）

中泰跨境物流系统，单体仓库：`apps/api`（Node + TypeScript + Prisma + PostgreSQL）、`apps/web`（Next.js 16.3.3 App Router，三个角色端 admin / staff / client，另有 agent）。这是**正在服务真实客户的生产系统**。

规矩（改代码前必看的）：`CLAUDE.md` 里有 37 条踩过的坑；`AGENTS.md` 是同类内容。**特别相关**：
- 第 22 条：前端手写类型 + 后端不再返回某字段 → 页面白屏（`Cannot read properties of undefined`）；
- 第 9/30 条：改动完要清死代码、冗余 import；批量改结构必须逐行看 diff；
- 第 21 条：列表不许静默截断；
- 第 23 条：`var(--xxx)` 只在主文档 CSS 里有效（canvas / 新窗口 / 导出文件里无效）；
- 唛头规矩：**显示客户一律显示「唛头」（= 客户账号 `users.id`），客户名字只在「客户管理」给内部看**；`scripts/test-mark-display.ts` 是这条规矩的看门测试。

## 2. 这次要审什么

### A（重点，**未提交**，还没经过任何独立审查）

工作区里的改动，`git status` 能看到：

```
 M .github/workflows/ci.yml
 M apps/web/src/components/admin/AdminOperationsOverview.tsx      ← 看板 3 处修补
 D apps/web/src/modules/layout/CountUpNumber.tsx                  ← 删掉（原引用已移除）
 M apps/web/src/modules/shipment/ShipmentPrintLabel.tsx           ← 打印标签加运单条码
 M docs/journal.md
 M package.json
 M scripts/test-admin-overview.ts                                 ← 29 项 → 42 项
 M scripts/test-hide-operator-identity.ts
?? apps/web/src/modules/shipment/trackingBarcode.ts               ← 新文件（条码生成）
?? scripts/test-shipment-label-barcode.ts                         ← 新测试（36 项）
```

内容：
1. **运单打印标签加条码**（新 `trackingBarcode.ts` + `ShipmentPrintLabel.tsx`）：同一运单各箱共用同一个**运单条码**；箱号仍是文字，不改变扫码后查运单的值；条码生成失败时有提示样式。
2. **运营看板 3 处修补**：两处「查看装柜管理 →」由 `<a>` 改成 `next/link` 的 `<Link>`；「查看运单管理 →」改成 `<Link href="#orders" onNavigate={…preventDefault() + navigateToHash("#orders")}>`（复用 `apps/web/src/modules/layout/navigate-to-hash.ts`，跟侧边栏同一路径）；删掉无引用的 `CountUpNumber.tsx`。
3. 测试与 CI：新增条码测试并进 CI；看板测试用**真的 `next/link`**（不再用 `<a>` 替身）、从页面 AST 抠真实 hashchange effect。

### B（**已上线**，9-20 06:59 上线的 `0fdecc0`，也过一遍）

`git show 0fdecc0`：运营看板从图表改为**直读数字**、柜子预警前置；新增 `apps/web/src/components/admin/AdminOperationsOverview.tsx` + 同名 CSS module；`apps/web/src/app/admin/page.tsx` 删了约 293 行图表代码；`apps/web/src/services/business-api.ts` 两个取数函数加 `AbortSignal`；新增 `scripts/test-admin-overview.ts`。

**已知的、已经修好的三个小问题**（别再当新问题报）：`<a>` 站内跳转、`CountUpNumber` 死代码、`/admin#orders` 要整页刷新才滚过去 —— 三条都在 A 里修了。

## 3. 已经做过的自测（别重复，但可以质疑）

- 全套 48 个脚本 + web/api 两套 `tsc` 全 rc=0；`test:admin-overview` 42/42、`test:shipment-label-barcode` 36/36。
- 我核过 Next 16.3.3 的 `node_modules/next/dist/client/link.js:95-105`：`onNavigate` 里调 `preventDefault()` 会跳过框架自己的 `router.push`。
- `CountUpNumber` 全仓库 0 引用。

## 4. 请重点看这些（我列的，但不限于）

**条码那批**
1. `trackingBarcode.ts` 生成的 HTML：**有没有转义 / 注入风险**？运单号里出现 `<`、`&`、引号、超长字符串、空串会怎样？会不会把标签页整段 HTML 弄坏？
2. 标签是 `document.write` 到**新窗口**打印的：新窗口里没有主文档 CSS —— 条码用的是内联 SVG 还是外链图片？**打印出来的标签会不会因为编码库没加载 / 异步来不及而空白**？有没有「先渲染完再打印」的时机问题？
3. 条码值用的是运单号：**扫出来的字符串必须能查到运单**（列印的号码与条码值要一致）；多箱共用同一条码是否符合原来的业务（箱号仍是文字）；条码失败时会不会把整张标签打成空白或抛错。
4. 条码生成库（`jsbarcode`）在**服务端渲染 / SSR** 或 `document` 不存在时会不会炸？（这个文件是不是只在浏览器里用？）
5. 有没有改到唛头 / 运单号 / 产品列表的输入值（**只许加显示，不许改数据**）。

**看板那批**
6. `Link href="#orders"` + `navigateToHash`：**前进/后退**、直接打开 `/admin#orders`、从别的页面带查询串进来、点两次同一个链接 —— 会不会把 URL 或分区搞乱？`onNavigate` 在中间键 / 新标签打开时会不会失效？
7. `AdminOperationsOverview` 现在是 `"use client"`：会不会影响它的服务端渲染 / 首屏？有没有把不该带到客户端的东西带过去？
8. 页面传进去的 `shipmentCounts`、`ordersLoaded`、`ordersError`、`overviewError`、`opsError` 的**组合状态**：有没有哪一格会显示成「0」或「没有异常」而实际上是「没加载到 / 加载失败」？（这是这个项目反复出问题的地方）
9. 看板数字的**口径**跟后端接口是否一致（别把「已加载的列表」当成全量），有没有把父单/子单算重、把不同状态算漏。
10. 删掉的 293 行里有没有**仍然被别处引用**的东西（组件、常量、类型、样式类）？`recharts` 依赖还要不要？

**通用**
11. 有没有引入「后端不再返回某字段、前端还在读」这种**白屏**风险（CLAUDE.md #22）？
12. 新加的测试**是不是真的在测东西**（真渲染 vs grep 源码；断言是不是能被换种写法绕过）？有没有测试在被改坏时**仍然会绿**的情况？

## 5. 输出

直接把结论写在你的回复里（不要写进仓库文件）。格式：

```
## 结论：能不能上线
（一句话）

## 发现的问题
### P1/P2/P3 - 一句话标题
- 文件:行
- 触发条件：
- 实测 / 推断：（实测就贴命令和输出）
- 建议：

## 我没能验证的
（列出来）
```
