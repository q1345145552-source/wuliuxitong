# Codex 复核提示词 — 尾端派送 / 派送单「品类不全」修复（2026-09-10）

> 用法：把「提示词正文」整段复制给 Codex，或让它读本文件照做。

---

## 提示词正文

你是湘泰物流系统的独立审查员。仓库 `/Users/liuyujiang/湘泰物流系统/MyWebSite`，分支 main，HEAD = `49126aa`（本地 = GitHub = 线上）。**审的是未提交的工作区**：`git diff HEAD` 加 2 个未跟踪文件 `packages/shared-types/product-names.ts`、`scripts/test-lastmile-product-names.ts`。`docs/sessions/adversarial-review-20260908.md` 是别的会话留下的未跟踪文件，不在范围内；`docs/journal.md` 只是记录。

### 老板报的问题

尾端派送页面信息好像不全（他不确定）；**导出的派送单肯定「品类不全」**——一票有好几个产品的货，单子上只印一个品名。

### 主修的诊断（你要独立核实，别直接采信）

- 员工建单 / 客户预报单在有产品行时，订单和运单的 `itemName` 只存**第一个产品名**：`apps/api/src/modules/orders/routes.ts` 里 `const primaryName = products[0].itemName`（客户预报单，约 :317）和 `const prName = staffProducts[0]?.itemName ?? body.itemName`（员工建单，约 :748）。批量导入 `batchOrderImport.ts:770` 是 `itemNames.join(" / ")`，所以批量导入的单没事。
- 下游把这个字段当整票品名用的地方：
  1. 尾端派送卡片 `GET /admin/lastmile/orders`（`admin-ops/routes.ts`）
  2. 客户签收单导出 `GET /admin/lastmile/customer-export-data`（同文件），分柜子单 / 分柜父单 `products` 故意为空、整票只有一行那条路
  3. 整柜拆柜派送清单导出 `GET /staff/loading-manifests/export-data`（`loading-manifests/routes.ts`），`products: []` 是故意的（不展开产品行、避免件数重复算回整票）
  4. 前端可派送运单候选列表 `fetchLastmileShipments`（`apps/web/src/services/business-api.ts`），取 `/staff/shipments?all=1` 的 `itemName`
- 生产日志只读核过：最近 10 天整柜清单导出 14 次、客户签收单 3 次，无 5xx。

### 主修的改法（只改显示，不动存的数据）

- 新增共享 `packages/shared-types/product-names.ts` 的 `productNamesLabel(products, fallback)`：按 `sortOrder` 排、去掉前后空格、同名去重、用「 / 」拼（跟批量导入一致）；没有产品行退回 fallback（原 itemName）。
- 上面四处改用它：① 列表接口的 `order.select` 多带 `products { itemName, sortOrder }`；② 客户导出的 `itemName` 换成拼接（`products` 展不展开的逻辑没动）；③ `loadOrderProductDims`（`orders/routes.ts`）的 select 多带 `itemName`/`sortOrder`、返回值多一个可选 `names`，整柜导出用它；④ 前端候选列表用 `s.products` 拼。
- 新增 `scripts/test-lastmile-product-names.ts`（7 项，真调三个路由 + `expandTemplateLines` 走到 Excel 行，Prisma 内存桩），接进 package.json 和 CI。
- 主修声称：三套 tsc 0 错；22 套 `npm run test:*` 全绿；变异自证——三处各退回老写法，对应用例红（列表→第 2 项、客户单→第 3/3b 项、整柜→第 4 项），恢复后字节一致。

### 任务一：诊断对不对

- 亲自核 `products[0]` 那两处，以及是否还有第三处建单/改单路径也这么存（编辑运单 `patch-shipment-bundle`、批量导入、预报单确认收货转正式单）。老板说「页面信息好像不全」——除了卡片和候选列表，尾端派送页还有没有别的地方显示品名。
- 主修没改的同症状点（它自己列的）：装柜明细 `loading-manifests/routes.ts:227`、柜子接口 `containers/routes.ts:186 / :1043`、列表接口 `shipments/routes.ts:497`、`admin/routes.ts:491`。判断：这五处有没有落在「尾端派送页面 + 两种派送单导出」这条路上被漏掉？如果有，报 P1；如果只是别的页面，按 P3 备注。

### 任务二：四处修复逐条销号（修对了没 / 修全了没 / 改坏别的没）

- **列表接口**：多带的 `products` 只有 `itemName`/`sortOrder` 两个字段？这个接口 8-22 因为 include 整表拖了 113MB 图（文件头注释），确认没有重蹈——`signImageBase64` 仍然不查。
- **客户签收单**：整票（不分柜）那条路展开产品行的行为一字未变？分柜子单 / 分柜父单只有一行、品名拼全？`expandTemplateLines` 的 fallback 行 `itemName: product.itemName || shipment.itemName` 拿到的是拼接后的？件数、方数、重量的取值**一个都没动**（CLAUDE.md #33：补显示字段不许顺带改数字）。
- **整柜清单**：`loadOrderProductDims` 改了 select 和 orderBy，长宽高「60/50」并排那套逻辑是否原样（test-lastmile-export 13 项要重跑）；返回值多了 `names`，另一个调用方 `admin/routes.ts:476`（管理员运单列表）拿到多余字段有没有副作用；`Object.keys(entry).length > 0` 的判断现在只要有品名就会 set，会不会让原来「没尺寸就不出现」的语义变成「出现但三个尺寸都 undefined」——前端 `dimOrNull(undefined)` 能不能兜住。
- **前端候选列表**：`/staff/shipments?all=1` 的响应里 `products` 是不是每条都挂上了（`shipments/routes.ts` 约 :100-125 按 orderId 挂）；子单的 `orderId` 有没有、能不能挂到产品行；`LastmileShipmentItem.itemName` 变长之后，卡片 / 搜索 / 批量匹配有没有被影响。

### 任务三：测试是不是真的

- `scripts/test-lastmile-product-names.ts` 是真调路由还是 grep 源码？Prisma 桩少了哪个模型会不会静默通过？把主修那三个变异在副本里重放，再加两个：`productNamesLabel` 改成只返回第一个名字；`loadOrderProductDims` 里去掉 `names`——看红不红。
- 跑 `test:lastmile-export`（13 项）、`test:status-group`、`test:product-rows`、`test:batch-import` 确认没回归。

### 规矩

- 可跑：三套 `tsc --noEmit`、22 套 `npm run test:*`（都不连库）、无库夹具。`next build` 只许在副本里跑（`rsync -a --exclude=node_modules --exclude=.next --exclude=.git --exclude='.env*' --exclude=.audit`），本仓库的 `apps/web/.next` 是老板正在用的 3000 dev 服务。
- 不许连数据库、不许起服务、不许碰 3000/3001、不许 git 写操作、不许改仓库文件（验变异在副本里改）。
- 报告先说「我实测过」还是「读代码推的」；查不到就说没查。

### 输出

① 诊断核实结论（对 / 不全 / 错，附 file:line）；
② 四处销号表（✅ / ⚠️ / ❌ + 依据）；
③ 新问题清单（P1/P2/P3 + file:line + 触发序列 + 建议修法），没有就写「没有」；
④ 实跑记录（命令、退出码、关键输出原样）；
⑤ 一句话：**可以提交 / 先修再提交**（提交不是部署，部署老板另定）。

---

## 第二轮：复核你报的 P1 / P2 / P3 是否修干净（2026-09-10）

> 用法：整段复制给 Codex。范围仍是 `git diff HEAD`（HEAD `49126aa`）+ 未跟踪的 `packages/shared-types/product-names.ts`、`scripts/test-lastmile-product-names.ts`。

你上一轮的结论「先修再提交」，主修按你的三条改了：

1. **P1 轨迹子单页签**：`apps/api/src/modules/containers/routes.ts` `GET /client/shipments/track` 的 `children[].itemName` 改成 `productNamesLabel(shipment.order?.products, cs.itemName) || null`（子单和父单同一张订单，拿父单查出来的 products 拼）。前端 `ShipmentTrackModal.tsx:400` 没动——它显示的就是这个字段。用你那套「真路由 + 离线真组件点子单标签」的夹具重放（`childItemName` 为「鞋」和为 `null` 两种），确认两个子单页签都显示「鞋 / 包 / 帽 ｜ N 件」，且件数仍是子单自己的。
2. **P2 测试防线**：`scripts/test-lastmile-product-names.ts` 重写：
   - Prisma 桩改成**严格**的：按 `select` / `include` / `orderBy` / `take` 语义递归裁剪返回值（`shape()`），没实现的模型 / 方法一被调到就抛错（Proxy）；
   - 新增第 5 项：真前端 `fetchLastmileShipments`（vm 里跑真 `business-api.ts`）→ 真 `GET /staff/shipments?all=1`，501 票翻两页，父单 / 子单 / 老单三种；
   - 新增第 6 项：真 `GET /client/shipments/track`，两个子单（itemName 分别为「鞋」和 null）。
   - 主修实跑变异 9 个：你那 8 个（M1–M8）+ M9「轨迹子单退回 `cs.itemName`」，**全部红**；恢复后字节一致、9/9 绿。你在副本重放 M6 / M7 / M8（上一轮的三个假绿）确认现在红；再想一两个新逃逸（比如桩的 `shape()` 对 `include` 的处理漏了某种写法、或 `/staff/shipments` 的 `orderProduct.findMany` 没 select 所以 M7 类变异在第 5 项照样绿——判断这算不算漏洞）。
3. **P3 长名省略**：候选列表和卡片的品名 `<span>` 加了 `title`（`LastmileDispatchWorkspace.tsx` 约 :414 / :662），CSS 没动。核一眼即可。

规矩同第一轮（不连库、不碰 3000/3001、build 只在副本、不改仓库文件）。输出：① 三条销号表；② 新问题（P1/P2/P3 + file:line + 触发序列，没有就写「没有」）；③ 实跑记录；④ 一句话：**可以提交 / 先修再提交**。

---

## 第三轮：只核你第二轮剩下的两个测试缺口（2026-09-10）

> 用法：整段复制给 Codex。范围不变（HEAD `49126aa` + 工作区）。业务代码这轮**一字未动**，只改了 `scripts/test-lastmile-product-names.ts`。

1. **P2-1 桩的 include 语义**：`shape()` 的 include 分支不再 `{...row}`，改成「标量全给、关系只给点名的」（`isScalar`：null / 非对象 / Date）；既没 select 也没 include 时同样只给标量。你那条「候选查询改成 `include: {}`」的变异重放，应当红（主修实跑：第 5 项红）。
2. **P2-2 直接断言查询参数**：桩把 `adminLastmileOrder.findMany` 收到的 args 记进 `lastmileQueries`，第 2 项直接断言：列表查询必须用 `select`、`select.signImageBase64 !== true`、products 子查询的 select 键恰好是 `["itemName","sortOrder"]`。你那条「select 加 `signImageBase64: true`」的变异重放，应当红（主修实跑：第 2 项红）。
3. 主修把 M1–M9 加这两条共 11 个变异重放了一遍，全红；恢复后七个文件字节一致、9/9 绿；`tsc --project tsconfig.scripts.json` 0 错。

输出：两条销号 + 有没有新逃逸 + 一句话：**可以提交 / 先修再提交**。
