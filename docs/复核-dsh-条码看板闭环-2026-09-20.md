# dsh 复核闭环：条码 / 看板待提交改动（2026-09-20）

## 结论

**当前代码可以提交，进入新 SHA / 新发布包流程。未提交、未推送、未上线。**

按用户要求直接调用本机 `dsh --profile headless` 连续三轮独立审查；Codex 没有派子代理，dsh 也声明未再分派。第三轮结论：F1/F2/F3/F4 闭环，无本轮代码阻断项。不是宣称已完成生产验收。

## 三轮结果与处理

| 问题 | 核实与修复 |
|---|---|
| F1：写 POST 挂起，使新加的 active 标记永久阻止账号视图更新 | 已复现并修复。`runAccountWrite` 统一四条路径：新建员工、新建客户、编辑客户、封禁/解封。30 秒等待截止，真实 AbortSignal 传到服务，finally 释放写标记和计时器。忽略 signal 的挂起也由 Promise.race 结束等待。 |
| F2：编辑客户旧快照覆盖保存后的新名字 | 已复现并修复。编辑路径接入同一写版本边界及写/刷新结果分类；原载荷和 agentId 仅真改才传的规则保留。 |
| F3：送审副本的三份记忆文档未同步 | **是复制疏漏，不是原仓库缺失。** 原仓库前轮四份文档仍逐字匹配其 manifest；补齐副本，第二、三轮均确认副本四份哈希一致。 |
| F4：唛头守卫把 `timeoutError.name` 误认成客户名字，CI 门禁红 | 修复实际 Error 对象的局部命名为既有允许的 `error`；四个引用同步，行为不变。**没有修改或放宽唛头守卫。** 恢复旧名则原守卫红，恢复最终版则绿；dsh 另注入真正客户名读取，守卫仍拒绝。 |

超时显示“请求超时，结果未确认，请刷新核对，勿重复操作。”：不清表单、不弹成功、不自动重试；迟到成功/失败不重入 UI 流程。**中断客户端等待不等于服务端回滚**，不声称已取消数据库提交。对网络断开等其他未知提交结果，没有新增后端幂等保证。

本轮只改三份业务/测试文件：
- `apps/web/src/app/admin/page.tsx`
- `apps/web/src/services/business-api.ts`（四个写函数新增可选 signal；不改 HTTP 路径、载荷或响应）
- `scripts/test-admin-overview.ts`

条码、标签、后端计数/趋势删除、员工占位工具删除、导航、CSS、依赖锁文件及已执行旧包本轮未再改。额外写本轮交接、journal、lessons、归档记录。

## 验证与证据口径更正

**必须更正**：fix-1 首次全量结果中 `test:mark-display` 实际 exit 1；我只核了末尾类型/构建结果，错误地称“全绿”。dsh 第二轮指出后已修复。原始失败完整保留在 `.audit/2026-09-20-dsh-review-loop/final-before-f4/` 和 `full-run-before-f4.log`，不是覆盖后当作一直通过。

最终实际验证：
- 看板 **108/108**，条码 **42/42**，唛头 **12/12**。
- **46 个 npm test 命令 + 3 套 tsc + 1 次隔离 Turbopack 构建，共 50 条，逐项核对非零结果为 0**；Next 16.3.3，30/30 页面。远程 CI 尚未触发，不说远程 CI 已通过。
- 汇总执行器已补上任一命令非零则失败退出；尾部实际 AST 用合成成功/失败结果验证 exit 0/1，脚本与验证记录一并留档。
- 最终源码 **10 个变异全拦截并逐字恢复**，恢复后 108/108；另做 F4 旧变量名变异，唛头红→恢复绿。
- 四条实际本机 HTTP→fetchWithSession→service→runAccountWrite 超时链验证通过；只收到一次写入，signal 中断、active/计时器释放，结果保持未确认。使用手控时钟触发真实 30 秒定时回调，不冒称四次墙钟等待。
- dsh 第三轮独立重跑看板/条码/唛头和三套 tsc 全过；独立 F4 红/绿对照、客户名泄漏反例及 5 项超时语义反例通过。它没有重复生产构建；构建证据来自本助手。
- 本助手及 dsh 均未将测试副本变异写回原仓库；最终三份源码哈希与 dsh 第三轮所审版本一致。

**未验而非通过**：`track-delete-db`、`push-ledger-db`、`agent-rebates-db` 三项独立 DB 脚本与 `agent-portal-isolation` 真接口段本轮跳过；用户曾自报通过，不作为本助手独立验证。实体打印机/扫码枪、生产账号 E2E、生产库、真实浏览器 confirm 分支仍未验。前轮已验证的两个合成浏览器时序不是生产 E2E。

旧有非阻断：改密码等其他写路径没有等待截止；唛头扫描器按变量名豁免 Error 存在固有宽松点。dsh 定性为范围外/非阻断，本轮没有扩大修改，更没有称“全部历史问题都已解决”。

## 提交与上线边界

1. 统一核对整批应提交的源文件/测试/文档；**必须包括两个未跟踪条码文件** `trackingBarcode.ts`、`scripts/test-shipment-label-barcode.ts`。
2. 固定新提交 SHA，另建新发布包，按既有备份/演练/停手/切换/回退流程验收；旧包和旧指纹不复用。
3. 真实打印扫码与生产验证依既有发布说明补验；不能用本轮本地绿色记录代替生产验收。

无自动提交/推送/部署，未读生产凭据或连接生产；原 3000/3001 服务保留。前轮临时测试服务已停，确认弹窗导致临时 IAB 标签页关闭失败的边界仍保留，本轮未绕过工具权限去操作 Codex。

## 可追溯材料

审查与完整日志：`.audit/2026-09-20-dsh-review-loop/`。
- `round-{1,2,3}.prompt.txt` / `.report.txt` / `.process.log` / `.exit.json`：三次真实 dsh 调用。
- `round-*-reviewer-scratch/`：dsh 独立反例及 A/B 日志。
- `final/results.json`、50 份日志、`full-run.log`：最终全量，尾部 `FINAL_FAILURES []`。
- `check-all.py`、`runner-finalizer-check.json`：已留档执行器与聚合退出校验。
- `fix-1/`、`fix-2/`、`mutations/`、`http-write-timeout.log`：原件、增量、变异及真实 HTTP。
- `final-source-hashes.json`：dsh 第三轮所审源码哈希。
- `current-task.patch`、`delivery-hashes.json`：本次闭环相对开始基线的三份源码/测试 + 四份收尾文档，**所有文档写完再生成最终清单**。
- `workspace-pending-manifest.json`：整工作区待提交清单，包含原有未跟踪文件；不是擅自批准将其他任务的无关文档全部提交。
- `final-verification.json`：原件+patch精确复现、源树/隔离副本哈希一致、原Git HEAD未变及非零结果汇总。

收尾文档是在第三轮源码复核后生成；纳入本轮最终 manifest，但不冒称 dsh 已审阅这些新收尾文字。原仓库没有 INDEX/ADR/术语表时依现有记忆文件交接，未改 AGENTS 协作约定。
