# 交 DeepSeek 核查：条码与看板 P2/P3 修复

## 状态与固定范围

五项本地修复已完成，等待独立核查；没有提交、推送、部署、数据库变更或实体打印/扫码。不要把本文件的实现方结论当作独立审查结果。

仓库：`/Users/liuyujiang/湘泰物流系统/MyWebSite`。HEAD 仍为 `f6fc8952828c13d3bddf4b1f76a2afbe170ea57e`。

工作区还包含之前获准的条码、导航、CountUpNumber 删除和员工占位标签删除。因此 **git diff HEAD 不是本轮独有差异**。本轮精确基线、原件与增量在：

- [本轮增量 patch](/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-20-final-p2-p3-fixes/current-task.patch)
- [前后 SHA256](/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-20-final-p2-p3-fixes/delivery-hashes.json)
- [修改前原件目录](/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-20-final-p2-p3-fixes/before)

## 已改内容与核查重点

1. **标签空格**：`ShipmentPrintLabel.tsx` 两个分支的运单号 span 使用专用 `.tracking-no { white-space: break-spaces; }`。不改其他文字、escapeHtml、原号编码、建单规则。请检查单/多产品分支及首尾/连续空格。
2. **写后刷新**：管理员页 `loadOverview(refreshAfterWrite)` 用 pending 位和共享完成 Promise 合并写后的刷新；新建员工、新建客户、封禁/解封三个调用点传 true，普通轮询不排队。请重点核实：旧 GET 未返回时写入；旧结果不落地；旧错误/超时仍补发；多个写合并；补发途中再写；最终失败通知所有等待者；同步抛错和完成微任务边界释放状态。写后刷新可能等旧请求完成/30秒超时，不承诺瞬时更新或≤10秒。
3. **趋势聚合**：删除管理员 overview 的 transitRows/映射/transitTrend，同步 `AdminOverview` 和接口文档。当前仓库没有 UI 消费者，monitor 只取 HTTP 状态；旧包 probe 不调用 overview，未修改/放宽。**stalledContainers 的 SQL 与全部计数/公司/父单/北京时间规则原样保留**，不是删除所有 status_logs 查询；未量生产 SQL 耗时。
4. **死 CSS**：只删 globals 的 `.kpi-flash`/`@keyframes kpiFlash`、ledger 的 `.kpi-flash`/`.ledger-kpi`及首项规则，其他 ledger 样式保留。
5. **测试边界**：注释明确 Node 的 next/link 是 `dist/client/link`，不是 App Router 构建 alias `app-dir/link`；没有冒称单元脚本覆盖真实框架导航。测试保留一份旧 transitTrend 额外字段夹具，是故意验证旧 API 回包也不会重新显示趋势，不是残留业务依赖。

## 实际验证与局限

- 新测试跑旧码：条码 **41/42**；看板 **42/53**，原有项目均通过，新增目标断言失败。先修 P2 后 **49/53**，再清 P3 **53/53**，追加同步异常/微任务边界后最终 **55/55**；条码最终 **42/42**。
- 基线和修后各运行 **46 个 npm test 命令**（AI数字命令内分别跑两个时区），以及 scripts/API/Web **三套 tsc**，均 exit0。**三项独立数据库脚本及 agent-portal 的真接口段因无测试库明确跳过**，不能说真库全绿。
- 最新源码隔离副本重新执行默认 Next 16.3.3 Turbopack 构建 **30/30**；复用既有独立依赖/缓存，没有在原工作区 apps/web 构建。
- **8项副本变异**：去 pending、放行旧结果、提前返回写等待者、去调用点 true、旧错误直接抛、去保留空格 CSS、恢复旧趋势 SQL、恢复死 CSS，均触发预期测试失败；逐项恢复并重跑条码/看板全绿。
- IAB 真标签 HTML：旧 `innerText` 只有一个空格，第二空格宽0；新单/多产品均保留两个空格且各约3.90px。不是仅看源码或会折叠空格的 DOM 摘要。
- 真实 Next 隔离夹具使用 AST 原样提取 loader/两种新建回调，内存 API 控制响应：员工旧 GET 成功、客户旧 GET 失败后都补发，页面最终员工4/客户112，等待标志解除，控制台无 warning/error。**非完整账号 E2E，不连接生产，不真实建单**。

证据：[全部检查结果](/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-20-final-p2-p3-fixes/final/results.json)、[变异结果](/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-20-final-p2-p3-fixes/mutations/results.json)、[标签浏览器](/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-20-final-p2-p3-fixes/label-browser-evidence.json)、[刷新浏览器](/Users/liuyujiang/湘泰物流系统/MyWebSite/.audit/2026-09-20-final-p2-p3-fixes/refresh-browser-evidence.json)。

## 后续边界

请先只读独立复核，再由用户决定是否修正/发布。实体打印机＋扫码枪试打老格式单号仍未完成；编码宽度依内容而非单纯长度决定，不能写成“13字以上必变细”。发布前需完整提交获准的新源码/测试文件，固定新 SHA、另建新包；已执行旧包不可重封或复用。
